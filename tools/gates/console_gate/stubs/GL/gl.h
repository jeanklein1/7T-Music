// STUB — declarations only, the web boot remains the runtime witness.
// glfw3.h reaches for <GL/gl.h> unless GLFW_INCLUDE_NONE is defined. The
// driver defines it; this file exists so the gate survives a future header
// that reaches anyway. Deliberately empty.
#pragma once

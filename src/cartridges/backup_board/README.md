# backup_board — frozen reference snapshot

Frozen reference snapshot of the_board (`837d920`, + the `a129960` namespace
rename and the `4b55d5e` 6-line harness stub). **NOT a build target** — the
`incubator_backup` target was removed at the sibling prune (it no longer built:
stale `musical/trajectory.hpp` include). Kept as a readable fossil record
(couplings, the waving mechanism, rudimentary forms).

**Excluded from sweeps/greps by design** — recons cite this banner instead of
re-deriving the exclusion. A symbol found only here is NOT a live reader.

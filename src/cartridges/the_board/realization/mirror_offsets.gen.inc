// GENERATED by tools/mirror_offsets.py — DO NOT HAND-EDIT (L28).
//
// THE MIRROR'S PER-FIELD WITNESS. Every line below is one member of a
// struct that world.wgsl registers with a `BYTE-FOR-BYTE (N B` marker,
// asserted at the offset THE WGSL ROOM puts it at. The number comes from
// world.wgsl and the check runs in the C++ compiler, so the two rooms
// prove each other rather than each proving itself.
//
// WHAT THIS CATCHES THAT NOTHING ELSE DID: a pad removed from one room
// and not the other, upstream of a 16-aligned member. Both rooms' SIZES
// stay identical — each pads implicitly back to the boundary — while
// every offset in between sits apart. The `sizeof` asserts cannot see it
// and neither can the binding ledger's 0b-4. THE_PANEL I U2 proved that
// by injection; U5 then hit it for real one unit later.
//
// Included from state.hpp after the mirrored structs are declared.


// ── SceneConstants  ↔  GPUSceneConstants  (3 members asserted) ──
static_assert(offsetof(GPUSceneConstants, tier_gains) == 0,
    "SceneConstants.tier_gains is at 0 in world.wgsl (array<AgentTierParams, 4>); the two rooms must agree field for field");
static_assert(offsetof(GPUSceneConstants, figure_profiles) == 192,
    "SceneConstants.figure_profiles is at 192 in world.wgsl (array<PawnFigure, 14>); the two rooms must agree field for field");
static_assert(offsetof(GPUSceneConstants, ribbon) == 4224,
    "SceneConstants.ribbon is at 4224 in world.wgsl (RibbonState); the two rooms must agree field for field");

// ── RibbonState  ↔  GPURibbonState  (22 members asserted) ──
static_assert(offsetof(GPURibbonState, anchor) == 0,
    "RibbonState.anchor is at 0 in world.wgsl (vec3<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, time) == 12,
    "RibbonState.time is at 12 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, cube_count) == 16,
    "RibbonState.cube_count is at 16 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, cube_size) == 20,
    "RibbonState.cube_size is at 20 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, height) == 24,
    "RibbonState.height is at 24 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, checker_scatter) == 28,
    "RibbonState.checker_scatter is at 28 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, color) == 32,
    "RibbonState.color is at 32 in world.wgsl (vec3<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, lateral_amp) == 44,
    "RibbonState.lateral_amp is at 44 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, lateral_freq) == 48,
    "RibbonState.lateral_freq is at 48 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, vertical_amp) == 52,
    "RibbonState.vertical_amp is at 52 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, vertical_freq) == 56,
    "RibbonState.vertical_freq is at 56 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, seed) == 60,
    "RibbonState.seed is at 60 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, propagation_speed) == 64,
    "RibbonState.propagation_speed is at 64 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, is_visible) == 68,
    "RibbonState.is_visible is at 68 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, orientation) == 72,
    "RibbonState.orientation is at 72 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, color_mode) == 76,
    "RibbonState.color_mode is at 76 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, is_wander) == 80,
    "RibbonState.is_wander is at 80 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, _pad1) == 84,
    "RibbonState._pad1 is at 84 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, wander_throttle) == 88,
    "RibbonState.wander_throttle is at 88 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, _pad3) == 92,
    "RibbonState._pad3 is at 92 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, color_b) == 96,
    "RibbonState.color_b is at 96 in world.wgsl (vec3<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPURibbonState, hue_spread) == 108,
    "RibbonState.hue_spread is at 108 in world.wgsl (f32); the two rooms must agree field for field");

// ── DesignConfig  ↔  GPUDesignConfig  (91 members asserted) ──
static_assert(offsetof(GPUDesignConfig, mute_dynamics_0d) == 0,
    "DesignConfig.mute_dynamics_0d is at 0 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, _pad_mute_signal_retired) == 4,
    "DesignConfig._pad_mute_signal_retired is at 4 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, mute_couplings) == 8,
    "DesignConfig.mute_couplings is at 8 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, pawn_speed) == 12,
    "DesignConfig.pawn_speed is at 12 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, freeze_sphere) == 16,
    "DesignConfig.freeze_sphere is at 16 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, fpv_mode) == 20,
    "DesignConfig.fpv_mode is at 20 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, world_seed) == 24,
    "DesignConfig.world_seed is at 24 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, sun_direction) == 32,
    "DesignConfig.sun_direction is at 32 in world.wgsl (vec3<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, aura_enabled) == 44,
    "DesignConfig.aura_enabled is at 44 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, pawn_aura_height) == 48,
    "DesignConfig.pawn_aura_height is at 48 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, fog_density) == 52,
    "DesignConfig.fog_density is at 52 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, fog_color) == 64,
    "DesignConfig.fog_color is at 64 in world.wgsl (vec3<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, world_bound_min) == 80,
    "DesignConfig.world_bound_min is at 80 in world.wgsl (vec2<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, world_bound_max) == 88,
    "DesignConfig.world_bound_max is at 88 in world.wgsl (vec2<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, placement_patch_count) == 96,
    "DesignConfig.placement_patch_count is at 96 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, terrain_time) == 100,
    "DesignConfig.terrain_time is at 100 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_blend_0) == 104,
    "DesignConfig.band_blend_0 is at 104 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_blend_1) == 108,
    "DesignConfig.band_blend_1 is at 108 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_blend_2) == 112,
    "DesignConfig.band_blend_2 is at 112 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_blend_3) == 116,
    "DesignConfig.band_blend_3 is at 116 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_blend_4) == 120,
    "DesignConfig.band_blend_4 is at 120 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_blend_5) == 124,
    "DesignConfig.band_blend_5 is at 124 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_phase_origin_0) == 128,
    "DesignConfig.band_phase_origin_0 is at 128 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_phase_origin_1) == 132,
    "DesignConfig.band_phase_origin_1 is at 132 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_phase_origin_2) == 136,
    "DesignConfig.band_phase_origin_2 is at 136 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_phase_origin_3) == 140,
    "DesignConfig.band_phase_origin_3 is at 140 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_phase_origin_4) == 144,
    "DesignConfig.band_phase_origin_4 is at 144 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, band_phase_origin_5) == 148,
    "DesignConfig.band_phase_origin_5 is at 148 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, mode_color_shift) == 152,
    "DesignConfig.mode_color_shift is at 152 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, mode_checker_scatter) == 156,
    "DesignConfig.mode_checker_scatter is at 156 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, mode_palette_target) == 160,
    "DesignConfig.mode_palette_target is at 160 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, mode_palette_intensity) == 164,
    "DesignConfig.mode_palette_intensity is at 164 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, mode_discrete_tier) == 168,
    "DesignConfig.mode_discrete_tier is at 168 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, mode_gol_tick_scale) == 172,
    "DesignConfig.mode_gol_tick_scale is at 172 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, mode_gol_height_scale) == 176,
    "DesignConfig.mode_gol_height_scale is at 176 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, floater_coordination) == 180,
    "DesignConfig.floater_coordination is at 180 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, pulse_count) == 184,
    "DesignConfig.pulse_count is at 184 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, possessed_slot) == 188,
    "DesignConfig.possessed_slot is at 188 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, pulse_data) == 192,
    "DesignConfig.pulse_data is at 192 in world.wgsl (array<vec4<f32>, 8>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, cull_point_x) == 320,
    "DesignConfig.cull_point_x is at 320 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, cull_point_z) == 324,
    "DesignConfig.cull_point_z is at 324 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, point_host) == 328,
    "DesignConfig.point_host is at 328 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, point_fly_speed) == 332,
    "DesignConfig.point_fly_speed is at 332 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, draw_ring) == 336,
    "DesignConfig.draw_ring is at 336 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, _pad_grain_band_retired) == 340,
    "DesignConfig._pad_grain_band_retired is at 340 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, _pad_veil_strength_retired) == 344,
    "DesignConfig._pad_veil_strength_retired is at 344 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, _pad_lod0_radius_retired) == 348,
    "DesignConfig._pad_lod0_radius_retired is at 348 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, palette_center) == 352,
    "DesignConfig.palette_center is at 352 in world.wgsl (array<vec4<f32>, 4>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, palette_light) == 416,
    "DesignConfig.palette_light is at 416 in world.wgsl (array<vec4<f32>, 4>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, palette_weight) == 480,
    "DesignConfig.palette_weight is at 480 in world.wgsl (vec4<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, checker_resultant) == 496,
    "DesignConfig.checker_resultant is at 496 in world.wgsl (vec3<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, checker_music_amount) == 508,
    "DesignConfig.checker_music_amount is at 508 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, checker_music_variance) == 512,
    "DesignConfig.checker_music_variance is at 512 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, point_bubble_radius) == 516,
    "DesignConfig.point_bubble_radius is at 516 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, cube_plasticity) == 520,
    "DesignConfig.cube_plasticity is at 520 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, pawn_tilt_tau) == 524,
    "DesignConfig.pawn_tilt_tau is at 524 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, fpv_eye_height) == 528,
    "DesignConfig.fpv_eye_height is at 528 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, field_slack) == 532,
    "DesignConfig.field_slack is at 532 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, field_k) == 536,
    "DesignConfig.field_k is at 536 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, field_fmax) == 540,
    "DesignConfig.field_fmax is at 540 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, _pad_field_occupier_gain_retired) == 544,
    "DesignConfig._pad_field_occupier_gain_retired is at 544 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, field_authored_gain) == 548,
    "DesignConfig.field_authored_gain is at 548 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, field_gain_cube) == 552,
    "DesignConfig.field_gain_cube is at 552 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, field_gain_sphere) == 556,
    "DesignConfig.field_gain_sphere is at 556 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, field_gain_agent) == 560,
    "DesignConfig.field_gain_agent is at 560 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, pawn_body_radius) == 564,
    "DesignConfig.pawn_body_radius is at 564 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_max_speed) == 568,
    "DesignConfig.ribbon_max_speed is at 568 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_yaw_rate) == 572,
    "DesignConfig.ribbon_yaw_rate is at 572 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_r_min) == 576,
    "DesignConfig.ribbon_r_min is at 576 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_floor_margin) == 580,
    "DesignConfig.ribbon_floor_margin is at 580 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_alt_smooth_dist) == 584,
    "DesignConfig.ribbon_alt_smooth_dist is at 584 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_alt_stiff) == 588,
    "DesignConfig.ribbon_alt_stiff is at 588 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_climb_rate) == 592,
    "DesignConfig.ribbon_climb_rate is at 592 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_mount_setback) == 596,
    "DesignConfig.ribbon_mount_setback is at 596 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_lookahead) == 600,
    "DesignConfig.ribbon_lookahead is at 600 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_clear_head) == 604,
    "DesignConfig.ribbon_clear_head is at 604 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_clear_body) == 608,
    "DesignConfig.ribbon_clear_body is at 608 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_hands_tau) == 612,
    "DesignConfig.ribbon_hands_tau is at 612 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_wander_soft) == 616,
    "DesignConfig.ribbon_wander_soft is at 616 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_wander_yaw_max) == 620,
    "DesignConfig.ribbon_wander_yaw_max is at 620 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_wander_arrive) == 624,
    "DesignConfig.ribbon_wander_arrive is at 624 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, ribbon_roam_radius) == 628,
    "DesignConfig.ribbon_roam_radius is at 628 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, camera_chase_ff) == 632,
    "DesignConfig.camera_chase_ff is at 632 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, camera_push_gain) == 636,
    "DesignConfig.camera_push_gain is at 636 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, camera_push_radius) == 640,
    "DesignConfig.camera_push_radius is at 640 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, _pad_arch_slack_retired) == 644,
    "DesignConfig._pad_arch_slack_retired is at 644 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, draw_mask) == 648,
    "DesignConfig.draw_mask is at 648 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, shadow_mask) == 652,
    "DesignConfig.shadow_mask is at 652 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, shadow_pcf_taps) == 656,
    "DesignConfig.shadow_pcf_taps is at 656 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, _pad720_1) == 660,
    "DesignConfig._pad720_1 is at 660 in world.wgsl (f32); the two rooms must agree field for field");
static_assert(offsetof(GPUDesignConfig, _pad720_2) == 664,
    "DesignConfig._pad720_2 is at 664 in world.wgsl (f32); the two rooms must agree field for field");

// ── FieldAuthored  ↔  GPUFieldAuthored  (3 members asserted) ──
static_assert(offsetof(GPUFieldAuthored, count) == 0,
    "FieldAuthored.count is at 0 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUFieldAuthored, _p2) == 12,
    "FieldAuthored._p2 is at 12 in world.wgsl (u32); the two rooms must agree field for field");
static_assert(offsetof(GPUFieldAuthored, rows) == 16,
    "FieldAuthored.rows is at 16 in world.wgsl (array<vec4<f32>, 8>); the two rooms must agree field for field");

// ── FieldBus  ↔  GPUFieldBus  (2 members asserted) ──
static_assert(offsetof(GPUFieldBus, ribbon) == 0,
    "FieldBus.ribbon is at 0 in world.wgsl (RibbonState); the two rooms must agree field for field");
static_assert(offsetof(GPUFieldBus, authored) == 112,
    "FieldBus.authored is at 112 in world.wgsl (FieldAuthored); the two rooms must agree field for field");

// ── AgentRoomConstants  ↔  GPUAgentRoomConstants  (2 members asserted) ──
static_assert(offsetof(GPUAgentRoomConstants, behaviors) == 0,
    "AgentRoomConstants.behaviors is at 0 in world.wgsl (array<AgentBehaviorParams, 10>); the two rooms must agree field for field");
static_assert(offsetof(GPUAgentRoomConstants, tier_gains) == 320,
    "AgentRoomConstants.tier_gains is at 320 in world.wgsl (array<AgentTierParams, 4>); the two rooms must agree field for field");

// ── FrameR  ↔  GPUFrameR  (5 members asserted) ──
static_assert(offsetof(GPUFrameR, lighting) == 0,
    "FrameR.lighting is at 0 in world.wgsl (Lighting); the two rooms must agree field for field");
static_assert(offsetof(GPUFrameR, vp) == 48,
    "FrameR.vp is at 48 in world.wgsl (VPMatrix); the two rooms must agree field for field");
static_assert(offsetof(GPUFrameR, camera) == 176,
    "FrameR.camera is at 176 in world.wgsl (CameraState); the two rooms must agree field for field");
static_assert(offsetof(GPUFrameR, sphere_pos) == 224,
    "FrameR.sphere_pos is at 224 in world.wgsl (vec3<f32>); the two rooms must agree field for field");
static_assert(offsetof(GPUFrameR, _pad_sphere) == 236,
    "FrameR._pad_sphere is at 236 in world.wgsl (f32); the two rooms must agree field for field");

// SKIPPED, WITH THE REASON (see the tool's banner, note 1):
//   FieldAuthored: 2 member(s) not declared under the same name in GPUFieldAuthored and therefore not asserted: _p0, _p1

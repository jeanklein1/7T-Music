> **ARCHIVAL** — this document describes the tree as of its own
> date and is superseded. Do not cite as current. Live facts live
> in the code; adjudications live in `audit/` and
> `docs/HANDOFFS/OPTIMIZATION 1/`.

═══════════════════════════════════════════════════════════════════════
COUPLING_SAGA — THE FINISHER (mop patch + floaters rename + T1 + flip)
Branch: continue on COUPLING_SAGA_SWEEP2
Companion file: mop.patch (apply from repo root, -p1)
═══════════════════════════════════════════════════════════════════════

TREE STATE (verified): Jean has confirmed the two upload rounds ARE the
current module state — the drift question is settled by his word. For
the record only (no gate): CC may paste `git log --oneline -8` +
`git show --stat` of the addendum commits to explain how 04 landed with
03b absent and 03a at two-of-three. Diagnostic, not blocking.

INHERITED LAW: unchanged (LF/no-BOM; witnesses pasted; one Jean-side
build at the end; commit order = risk ascending, sole behavior-bearing
edit last).

COMMIT PLAN
  F1  MOP        apply mop.patch (comments + one free code rename)
  F2  FLOATERS   floater_vocabulary.hpp → floaters.hpp (mv + sweep)
  F3  T1         TileGrid runtime-sizing (completes the twin set)
  F4  FLIP       PATCH_PREGEN_RADIUS 7 → 8 (DISCLOSED — the weld)

───────────────────────────────────────────────────────────────────────
F1 — MOP (apply the patch)
───────────────────────────────────────────────────────────────────────
The mop was performed by Claude against the verified snapshots: 200
count-checked surgeries across 29 files (9 untouched: demo_config,
drawable_table, ground_architecture, mood_constants, pawn,
render_passes, seed_utils, terrain_looks, wgpu_fwd — pawn.hpp needed
nothing; the wing's exemplar).

Classes closed: the orphaned coordinates (m#, m3b, p1a/p1b-x, p2, d2,
C2/C7/C8, R1/R5 recon ids, husk-sweep and residue-T2 parentheticals,
L2-a, M-j, A-era, "recon §" refs); the fossils (both "Keyhole form"
sites → "Root-called owner verb", theory-v2 ×3 → "the program theory",
the doubled state.hpp include comment, the spheres self-referential
rename casualty → "renamed from ActiveFloater"); the C3 builder tags →
"THE SHARED BUILDER(S)"; and the free code rename
CubeEntityProp → CubeProp (26 tokens, two files, values pinned by the
standing static_assert).

KEEP survivors, by design: C6 (the registry's name), the cartridge's
live phase ids R1..R21, Option A, "Lifecycle Phase 2", every law id
(O/RC/E/F/K/P/S/Q, b-rulings, V1), all SEAM/STATUS/LATENT/INTENT/
TESTING tags, all constitution § refs, F3/F4 face ids.

Steps:
  git apply --check mop.patch     # must be silent
  git apply mop.patch

WITNESS (F1):
  grep -rEn 'p1[ab]|husk sweep|residue T2|theory v2|A-era|Keyhole form|L2-a|M-j|\(m4\)|\(m3|\(R1\)|CubeEntityProp' cartridges/the_board | grep '//' | wc -l   # 0
  Comment-only proof: every changed line lacking '//' is a CubeProp
  token line —
  git diff HEAD~1 | grep -E '^[+-][^-+]' | grep -v '//' | grep -vc 'Cube\(Entity\)\?Prop'   # 0
  glaw1 GREEN (CubeProp is code; the pin assert recompiles).

───────────────────────────────────────────────────────────────────────
F2 — FLOATERS (the tier's last self-declared debt)
───────────────────────────────────────────────────────────────────────
  git mv cartridges/the_board/contracts/floater_vocabulary.hpp \
         cartridges/the_board/contracts/floaters.hpp

Sweep (grep-driven, grounded-rename precedent):
  grep -rn "floater_vocabulary" cartridges/the_board
  → every include line + comment mention → floaters.hpp
  (expected sites: spheres.hpp include, cube_behaviors include if any,
   the file's own banner title line, spine/spawn comment mentions).
Banner: the title line renames; the LATENT[naming] debt paragraph DIES
(the debt is paid); the History line stays.

WITNESS (F2):
  grep -rn "floater_vocabulary" cartridges/the_board | wc -l   # 0
  glaw1 GREEN.

───────────────────────────────────────────────────────────────────────
F3 — T1 (TileGrid runtime-sizing; completes the twin set)
───────────────────────────────────────────────────────────────────────
The one remaining fixed twin (world.wgsl ~:903):
  entries: array<TileGridEntry, 289>  →  entries: array<TileGridEntry>
Rules carried from the addendum, scoped to T1:
  - Last-member storage-array (verified) — legal, and the file's own
    dominant pattern.
  - The "Twin of CPU Dim::…" annotation (if present) dies for the
    structural truth: capacity is the bound buffer's; the CPU side is
    the single source; no WGSL twin exists.
  - Any literal 289 bound over TileGrid → arrayLength(&…) transform,
    value-identical at radius 7; expected: none (indexing rides the
    runtime side field — the census's finding).
  - C++ verify, no edits expected: no pinned minBindingSize on the
    tile-grid layout entry.
DISCLOSURE: none — behavior-identical at radius 7 by construction.

WITNESS (F3):
  grep -n "array<TileGridEntry" realization/world.wgsl   # runtime form, no count
  GATE: Jean build — Dawn + FXC clean; static rig pixel-identical.

───────────────────────────────────────────────────────────────────────
F4 — FLIP (the weld proper) — DISCLOSED, LAST
───────────────────────────────────────────────────────────────────────
Now unblocked: all three twins runtime-sized (T2/T3 already landed;
F3 closes T1). The chain is fully derived (STEP-1 finding stands):
RADIUS is the one dial; SIDE/MAX/TILE_* follow; the pregen assert is
already `>=` and holds at 8×50=400 ≥ 350. EXIST/RING/LOD0 untouched.

  [a] state.hpp Dim: PATCH_PREGEN_RADIUS 7 → 8   (one character)
  [b] Comment sweep: the "(15×15, 350 world units)" tail on the dial's
      line updates to the derived truth (17×17, 400); the veil note's
      "PREGEN-8 … flagged, not started" resolves to landed; any other
      stale "(7)"-class numeric in touched comments — listed.
  [c] Literal-twin re-check on the post-F3 tree: grep 225/289/17/19 in
      state.hpp + surface files + world.wgsl; classify; expected zero
      unresolved. Tooling re-check: clean (already verified).

DISCLOSURES (commit message): world content identical
(seed-deterministic; depth changes WHEN, not WHAT); boot fill ~28%
longer (289 layers vs 225, same budgets); GPU memory +64 patch layers;
steady-state identical.

WITNESS (F4): glaw1 GREEN, recomputed values pasted (SIDE 17, MAX 289,
TILE 19/361).

───────────────────────────────────────────────────────────────────────
CLOSE-OUT
───────────────────────────────────────────────────────────────────────
One build after F4. Acceptance ladder: static rig pixel-identical
(F1–F3 are inert on screen) → the rim flight (F4's reason to exist —
pops gone is the pass) → the zones stay planted (already landed, just
confirming nothing regressed). Push; SWEEP2 merges to the trunk on
green. Jean-side leftover unchanged: the origin/COUPLING_SAGA deletion
(one click; LEGACY holds the lineage).

After green the tree is finished-clean: zero orphaned coordinates,
zero fossils, every twin runtime-sized, the radius welded, the tier's
naming debt paid. The queue holds only row one.
═══════════════════════════════════════════════════════════════════════

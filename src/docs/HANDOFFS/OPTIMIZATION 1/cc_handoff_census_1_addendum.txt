════════════════════════════════════════════════════════════════════
7T — CENSUS_1 — SEED ADDENDUM (append to [5])
Four seeds from the 2026-07-28 design-side recon. Same rules as the
parent handoff: verify, classify, dispose on paper; hands off the
tree.
════════════════════════════════════════════════════════════════════

7. world.wgsl, shadow-constants block:
     const SHADOW_MAP_SIZE: f32 = 4096.0;
   The SECOND HOME of Dim::SHADOW_MAP_SIZE (state.hpp). Both PCF
   kernels derive texel_size from it. A Dim fact with two homes, kept
   true only by manual lockstep — CLASS C in spirit even while the
   numbers agree. PROBE_1 rev2 C2 edits both rooms in lockstep; the
   census rules the PERMANENT home (pipeline-override constant piped
   from Dim:: vs a twin pair with a mirror note, the TILE_GRID_
   CAPACITY pattern). The wrong ruling it invites: "change Dim:: and
   the map follows" — which would silently shrink every PCF world-
   footprint.

8. The indirect-draw claim: "one indirect draw per pass". At least
   three homes read design-side: state.hpp (frustum buffer creation
   comment), render_passes.hpp (main-pass LOD1 comment), and its
   corollary in draw_shadow_all — "One DrawIndexedIndirect budget
   line free in this pass (L2.4)". Count with a boundary that catches
   the corollary phrasing, not only the verbatim sentence. dawn.json
   and the WebGPU spec place drawIndexedIndirect as an ordinary
   per-call encoder method — the one-per-CALL limit belongs to the
   multi-draw FEATURE (value 50), not to plain indirect draws.
   Settle whether any Dawn version this tree ever ran had the
   limitation. Either way the claim is LOAD-BEARING: it is why LOD1
   draws its full annulus uncullable and why the shadow pass culls
   nothing. Wrong ruling produced: the draw-submission architecture
   itself. Rank at the top, beside seed 2.

9. state.hpp: terrainIndexBuffer_ ("Terrain IB") + its comment
   "filled once by compute shader, read every frame", and the
   generate_terrain_indices entry point (world.wgsl) that feeds it.
   The patch system appears to have replaced its consumer. Verify the
   reader set with a proper boundary. Suspected CLASS B (buffer +
   kernel) carrying a CLASS A/C claim ("read every frame"). If dead:
   ~2.4 s of FXC boot time and a storage binding ride along with it.

10. world.wgsl, coupling_pawn_to_sun_vp: "Shadow map content is
    pixel-perfect between grid crossings, enabling the CPU to skip
    the shadow pass on idle frames." Verify whether ANY CPU site
    exercises the skip — the METER shows ShadowPass paying full cost
    through resting windows, so the evidence says no. If unexercised:
    a capability presented as enabled. CLASS A or C — and LEVER-
    BEARING: GEOMETRY_2's rate-aligned shadow map (cache static-
    caster depth, redraw movers only) is this sentence grown up. The
    census reports; the campaign decides.

SCOPE NOTE (Jean's ruling, optional): src/console/** sits outside the
declared scope, yet the adapter finding shows the host layer's prose
carries the same disease. Adding it costs ~one file. The four seeds
above all fall inside the existing scope regardless.

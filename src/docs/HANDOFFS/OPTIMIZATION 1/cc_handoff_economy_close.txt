════════════════════════════════════════════════════════════════════
7T — ECONOMY_1 — THE CLOSE
Run AFTER Jean's gate passes the draw plan. One merge, one ledger,
one record. This handoff ends the campaign.
════════════════════════════════════════════════════════════════════

GIT LAW: master. Three commits in order. PROCESS_LAWS govern.

────────────────────────────────────────────────────────────────────
U1 — MERGE claude/economy-e3-draw-plan
────────────────────────────────────────────────────────────────────
Adjudication in the message per Jean's gate report (identity verdict,
settled CULL_MARGIN_WU if he tuned it, footnote numbers). Merged
branch prunable. If Jean reported a settled margin value, pin it in
the same commit.

────────────────────────────────────────────────────────────────────
U2 — THE CLOSING LEDGER (charter amendment; Jean stamps, diff at top)
────────────────────────────────────────────────────────────────────
Append to ECONOMY_1_CHARTER.md a final section, verbatim skeleton
below, CC fills dates and the merge hashes:

## THE CLOSING LEDGER
| Entry | Verdict | Reason |
|---|---|---|
| E1 curtain switch | LANDED (rev2), then SUPERSEDED into the draw plan | global flag -> per-patch selection; snapshot keeps the flag |
| E2 shadow at half mesh | LANDED | shadow GPU 6.37 -> 3.40 at rest; identity held |
| E3 LOD1 through the cull | LANDED (as the draw plan) | first frustum test LOD1 ever had |
| E3b shadow vs sun box | DEAD | post-E2 the whole pass is ~3.4 ms; the machinery exceeds the prize |
| E4 indoor light economy | PARKED | CPU witness was Debug-inflated; re-rank after the Release record, owner: a future indoor pass |
| E5 margin as ratio | LANDED (inside the plan) | the planar margin guarded XZ motion that does not exist (R1) |
| E6 ratios | LANDED (SWEEP_1) | bias constants born again as ratios |
| E7 heightfield at reader rate | MOVED | to the memory pass, still gated by audit A1 |
| E8 shadow at change rate | DEAD | 16 MB/frame copy vs a 3.4 ms pass; economy honesty |
| E9 the ring | HORIZON | unchanged; gated behind this record |
| E10 painting residency | MOVED | to the memory pass; Class III absolute stands |

Plus two lines: SHADOW_QUALITY_1 (b3648f3) and the Vulkan fast-boot
(b09885e) are recorded as the aesthetics chapter's opening pieces,
held and untouched. Diagnostics remain as-is by Jean's ruling; the
census cadence constant's home is named here for whoever quiets it
later (locate and cite it — name only, no edit).

────────────────────────────────────────────────────────────────────
U3 — THE RECORD (appended when Jean pastes the Release boot)
────────────────────────────────────────────────────────────────────
Jean will build ONCE in Release on final master and paste the log
into this session. Append to the closing ledger, from that log:

  THE RECORD (Release, 920M/D3D12, Dawn f0bf8ab5, <date>):
  - the platform block verbatim (adapter, revision, Build: Release);
  - outdoor rest: fps, main/shadow GPU mean;
  - one traveling window; one indoor window;
  - beside them, the campaign's opening numbers for contrast
    (HD 5500, Debug, unchosen adapter: 19.2 fps, main 22.3,
    shadow 10.3).

One commit: "ECONOMY_1: closed — the record". Then list every
prunable branch for Jean in the report, and stop. The campaign is
over; the aesthetics chapter opens on the shadow branch.

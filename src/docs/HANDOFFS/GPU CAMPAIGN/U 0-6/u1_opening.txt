═══════════════════════════════════════════════════════════════════════
CC HANDOFF U1 — THE OPENING COMMITS (certified cleanup + stale six)
Everything here was certified or enumerated by AUDIT-2. No design.
═══════════════════════════════════════════════════════════════════════

COMMIT [U1a] — the two certified corpses (A2-2, patch model: A2_P1)
1. Apply the A2_P1 cleanup as REAL edits (re-derive from the patch,
   verify each hunk against the tree): the photo_patch_instances = 144
   registry constant + stragglers; the trajectories corpse in full —
   buffer member, creation, uploads, CPU feed structs, the surviving
   WGSL @binding(101) declaration, world.wgsl §1.2 Trajectory /
   trajectory_release (zero callers), and the bonus corpse
   Idle::TRAJECTORY_FIELD_* (zero uses). glaw1.

COMMIT [U1b] — the stale six (A2-2c, list is exhaustive)
2. binding_registry.hpp:71/73 — delete the two dead alias notes
   ("aka fc_agents" / "aka fc_camera").
3. binding_registry.hpp:28–29 — the census parenthetical becomes:
   "(101 declarations over 96 slots at Stage-5 opening — audit A2;
    three fc_ aliases share slots: fc_config/fc_vp/fc_patches)"
   — then RE-COUNT after [U1a]'s @binding(101) removal and write the
   ACTUAL numbers (expected 100 declarations); log the count recipe.
4. state.hpp:3893 + 4862 — shadow texture banners: "bindings 22-23,
   26-28 + 34 (4 entries…)" — write the true current set (verify).
5. state.hpp:3927 + 4887 — render texture banners: append g1:34 to the
   enumerations; entry count 10→11.
6. state.hpp:4751 — render entity group banner "19 entries" → the true
   count (18; verify against the array size).
glaw1 after [U1b].
═══════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════
CC HANDOFF C4 — CONTACT_2 CLOSEOUT
═══════════════════════════════════════════════════════════════════════
1. Instruments → _post_c2: cc6 (NO layout deltas expected anywhere —
   this batch added zero bindings; flags EMPTY), cc7 (+0 declarations
   unless the config field's mirror counts — record the truth), cc4
   (agent kernels' closures unchanged in bindings; paste the sets),
   Dawn witness ALL FAMILIES GREEN (the edited kernels revalidate).
2. BATCH_REPORT_C2.md: recount · the C1a geometry-law deltas · the
   λ = 0 bit-neutrality statement · the flock re-point equal-value
   proof · the bubble graduation disclosure (portal sensitivity now
   rides the same radius) · the camera-velocity VERIFY outcome
   (field found vs fallback) · deviations · encoding sweep.
3. Push. JEAN'S GATE LIST (Windows, one session):
   [ ] Build + boot. FXC watch: nothing structural changed — terms in
       existing kernels; the gathers grew by a flee block (still one
       bounded loop each).
   [ ] REST IDENTITY, bitwise: λ = 0, flee gated on approach, steering
       gated on speed, bubble rest 20 ⇒ a still world is the pre-batch
       world.
   [ ] THE GEOMETRY WALK: stand atop a MONOLITH plateau — agents at
       the base stay calm; fly a cube overhead of the pawn — it feels
       nothing until spheres truly meet.
   [ ] THE WHISPER WALK: walk straight at an antenna — the path bends
       before the clamp ever fires; dead-center approach still stops
       (the wall behind the whisper). Watch agents wander AROUND
       columns.
   [ ] THE MATADOR: sprint at a sentinel — it clears you at your
       speed, sideways, then settles back to its gait. Sprint through
       a crowd — a wake, not an explosion.
   [ ] THE POINT: free-fly low through a crowd — it parts around the
       camera (the bubble, host-routed); climb — the parting stops
       (the vertical gate). Portal skim still fires as before
       (radius unchanged at rest).
   [ ] PLASTICITY (tuning, not gating): set one cube tier's λ to 0.5
       locally — shove a cube, watch it stay shifted, drift spring
       softening the transit; revert or keep per taste (the column is
       yours).
4. DEFERRED REGISTER (named, not lost): living-plateau steering (needs
   a GoL gradient channel — Layer-E-adjacent) · per-slot floater radii
   (pending a size field in the fe struct — C0 anchor a/j census
   decides) · camera-velocity upgrade if the fallback shipped · the
   bubble's coupling wire (the coupling campaign's panel) · the ribbon
   floor channel (the presence card's first citizen — its own small
   campaign) · emissaries (the glider travelers — likewise).
═══════════════════════════════════════════════════════════════════════

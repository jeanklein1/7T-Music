═══════════════════════════════════════════════════════════════════════
CC HANDOFF C0 — CONTACT_2: INDEX + PREFLIGHT
Campaign: CONTACT_2 (the composed-spheres completion: geometry law,
steering, the social split). Branch: CONTACT_2, cut from the trunk Jean
designates (record hash). Laws identical to H0/U0/T0 (CLASS A/B,
LF/no-BOM, FXC banner untouched, per-edit log CONTACT_2_LOG.md, glaw1
per handoff, one Windows build at batch end, push at C4).
No new bindings anywhere in this batch — every edit is kernel terms,
tier columns, one config field, and boot plumbing.

LAND ORDER: [C0] → [C1] → [C2] → [C3] → [C4].

AUTHORED DEFAULTS (Jean-tunable in-row; veto by editing the row):
flee gains vs the player per tier {worker 1.2, scout 1.4, sentinel 1.0,
leader 1.1} · NONPLAYER_FLEE_GAIN 0.8 (< 1.0 — the cascade-contraction
law) · STEER_LOOKAHEAD_WU 4.0 · cube plasticity λ = 0.0 on every tier
(bit-rest; the dial ships armed but at rest).

C0 PREFLIGHT — anchor table (expect stated counts):
  a. world.wgsl the T2 gather blocks: grep "CONTACT_SPRING" (def + its
     use sites; paste the two gather blocks + the cube repulsion block
     — they are this batch's primary edit surface, CC-authored at TC1)
  b. world.wgsl "fn update_cube" (1) — paste the home+drift integration
     lines (the anchor/drift fields by their true names)
  c. world.wgsl "PAWN_FORCEFIELD_RADIUS" (def) + the flock sensing
     radius const in behavior_flock2d (paste both)
  d. world.wgsl "POINT_BUBBLE_RADIUS" (def + every reader — expect the
     portal sensor's vertical gate; paste each)
  e. world.wgsl "fn point_pos" (1)
  f. state.hpp GPUDesignConfig — paste the growth-law banner + the
     nearest pad/append point (C3 adds one field)
  g. camera_state velocity: paste the GPU camera struct — VERIFY a
     velocity field exists (C3's host-routed flee source); if absent,
     the documented fallback in C3 applies.
  h. The cube tier table (CUBE_POPULATIONS or its true name) — paste
     the row struct (C1 adds the plasticity column).
All resolvable → C1. glaw1 baseline GREEN first.

═══════════════════════════════════════════════════════════════════════
CC HANDOFF C1 — THE GEOMETRY LAW + PLASTICITY
Two corrections to the landed contact, before any new emitter is born
under them.
═══════════════════════════════════════════════════════════════════════

COMMIT [C1a] — 3D gates, planar response (all landed pairs)
1. CLASS B — in EVERY pair test from anchor (a) — both walker gathers'
   agent loop + sphere loop, and the cube-vs-pawn repulsion — the gate
   distance becomes 3D while the response stays planar:
     let dy = self.pos_y - other.pos_y;      // true field names
     let d2 = dx*dx + dy*dy + dz*dz;         // GATE: the sphere is a sphere
     ... if (d2 < r*r && d2 > 0.0001) {
         let d_pl = sqrt(max(dx*dx + dz*dz, 0.0001)); // RESPONSE: planar
         ... (dx / d_pl), (dz / d_pl) ...
   Overlap for the spring uses r − sqrt(d2) (the true 3D overlap);
   direction uses the planar normal. Comment at the first site, cited
   at the rest: "3D gate, planar push — influence is vertically bounded
   (the bubble's portal-gate law, contracts/point.hpp); walkers are
   never pushed into the air; a cube overhead feels nothing from a pawn
   beneath (CONTACT_2)."
2. Behavior deltas to log: a pawn atop a MONOLITH plateau no longer
   shoves agents at its base; airborne cubes ignore ground bodies
   outside their sphere. Ground-level pairs: dy ≈ 0 ⇒ unchanged.

COMMIT [C1b] — cube plasticity (the anchor leak)
3. CLASS B — the cube tier table (anchor h) gains a `plasticity`
   column, 0.0 every row ("λ: 0 = elastic (today, bit-exact); 1 =
   fully sculptable; Jean-tunable"). Mirror the column into the GPU
   row struct via its pads if present, else per the table's own upload
   shape (paste before/after; size pins untouched or bumped in
   lockstep).
4. CLASS B — update_cube (anchor b), immediately AFTER the drift
   integration line:
     let leak = clamp(tier.plasticity * zone... /* the tier's λ */
                * signal.dt, 0.0, 1.0);
     anchor += drift * leak;
     drift  -= drift * leak;
   using the struct's true field names. Banner comment: "PLASTICITY —
   displacement leaks from drift (temporary) into anchor (permanent);
   pos = home + drift is unchanged in the instant, so the transfer
   moves no pixels: the present becomes the next configuration's
   initial condition (the continuity law). λ = 0 reproduces the
   elastic cube bit-exactly. GPU anchor mutation precedent: the
   kite-release anchor freeze."
5. Rest gate note for C4: λ = 0 everywhere ⇒ this commit is
   bit-neutral by construction.
glaw1 after [C1b].
═══════════════════════════════════════════════════════════════════════

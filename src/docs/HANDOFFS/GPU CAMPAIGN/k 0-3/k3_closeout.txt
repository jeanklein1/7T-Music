═══════════════════════════════════════════════════════════════════════
CC HANDOFF K3 — CONTACT_3 CLOSEOUT
═══════════════════════════════════════════════════════════════════════
1. Instruments → `_post_c3`: cc6 (no layout deltas — zero bindings
   added; flags EMPTY), cc7 (+0 declarations), cc4 (agent/cube kernel
   closures unchanged), Dawn witness ALL FAMILIES GREEN. Report the
   `update_other_agents` pipeline-creation TIMING vs the previous
   witness run — the K1 split moved the ground chain from ~10 inlined
   sites to one and may show as a compile-cost drop.
2. BATCH_REPORT_C3.md: the diagnosis restated (the cap ate every
   imposed motion) · the split · the three tuning changes with their
   worked examples · deviations · encoding sweep · THE TUNING TABLE
   below, so Jean has every knob in one place with its file:line.

   THE TUNING TABLE (fill file:line for each):
     FLEE_SHELL_FRAC ........... flee trigger as a fraction of sensing
     personal_radius (×4 tiers)  sensing + (×frac) flee shell
     flee_gain_player (×4) ..... escape gain vs the point
     NONPLAYER_FLEE_GAIN ....... body-to-body escape gain (<1: wakes)
     CONTACT_SPRING / _IMPULSE_CAP  the shove
     contact_radius / contact_mass (×4)  the bodies
     PAWN_CONTACT_MASS_MULT .... the player's authority
     CUBE_PART_RADIUS / _CAP / _GAIN  the parting
     CUBE_PLASTICITY_DEFAULT ... the live λ master
     CUBE_TIER_GAINS.plasticity (×4)  per-tier λ character
     STEER_LOOKAHEAD / _GAIN / _GRAD_LO / _HI  the whisper
     config.pawn_speed ......... the player's speed (the flee's input)

3. Push. JEAN'S GATE LIST (Windows):
   [ ] Build + boot. FXC watch: agent_settle is a MOVE of existing
       code, not new code; the gathers are unchanged in shape.
   [ ] TERRAIN REST IDENTITY (still a gate): a still world's terrain
       is unchanged — the split touches no terrain path.
   [ ] THE MATADOR, retried: sprint at a sentinel. It should now clear
       you at YOUR speed and settle back to its gait. This is the gate
       K1 exists for — if it still fails, the sign and the seam are
       both verified, so the next suspect is `config.pawn_speed` vs
       the agents' `speed_cap` being so far apart that even a correct
       escape reads as sluggish; report the two numbers.
   [ ] THE CROWD: walk (don't sprint) through a cluster. Agents should
       hold formation and part locally — no permanent nervousness.
       If they still mill, drop FLEE_SHELL_FRAC further.
   [ ] THE CUBES, three things: shoulder one — it parts (not flings)
       and STAYS moved; walk past at range — nothing happens until ~8
       wu; sweep through the drone show and rearrange it. Then tune
       CUBE_PLASTICITY_DEFAULT to taste (0 = old elastic behavior).
   [ ] The whisper + the geometry gates from C2, unchanged.
4. DEFERRED REGISTER (carried + new):
   - figure-scaled contact radius (CLOSURE_PAWN interaction: a
     Colossal and a Spire share a tier's body radius) — the clean fix
     is widening binding 112's visibility to COMPUTE.
   - CPU-authored point velocity (`config.point_vel_x/z` from the P5
     harvest) — retires the camera-host BUBBLE_PART_SPEED fallback and
     restores "a still camera parts no one".
   - the player's gather above the ground-resolve tail (if a shove
     sink is ever visible).
   - AgentState pad debt (96→112) blocks per-agent individuality.
   - living-plateau steering · per-slot floater radii · the bubble's
     coupling wire · the ribbon floor · emissaries.
═══════════════════════════════════════════════════════════════════════

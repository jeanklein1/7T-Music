═══════════════════════════════════════════════════════════════════
CC HANDOFF — SPAWN_1  (census: resurrection → rebuild → prediction)
Campaign: SPAWN_SWEEP (spec: spawn_campaign_v2.md — READ IT FIRST)
Cartridge: the_board (incubator_dual)
Base: master @ 3ca9498
Prior: SPAWN_0 landed (ac08f05, db326d2). Part B audit accepted whole.
Encoding: LF-only, no BOM. Do not introduce CRLF.
═══════════════════════════════════════════════════════════════════

Your Part B audit is the source for every line reference below. Where
this handoff and your report disagree, YOUR REPORT WINS — it was
derived from the tree, this was derived from your report.


═══════════════════════════════════════════════════════════════════
GIT LAW — MASTER
═══════════════════════════════════════════════════════════════════

Trunk-based on `master`. Every commit lands on master and pushes
straight to GitHub, so the local tree, GitHub, your checkout, and
project knowledge always resolve to ONE lineage.

History lives in COMMITS, not branches. Inspect with log / show /
bisect. Tag states worth finding again rather than branching them.

`claude/*` branches are transient review handoffs. Merge to master and
delete the same day. They are never lineage.


─── BEFORE REASONING ABOUT ANCESTRY ───

    git rev-parse --is-shallow-repository

Expect `false`, and ~951 commits.

LAST SESSION THIS RETURNED `true`. The clone WAS shallow and the law
fired exactly as written. Assume nothing; run it again.

A shallow clone MANUFACTURES ARTIFICIAL ROOTS and makes `merge-base`
report unrelated histories on a repository that has exactly one. That
misreading cost a full session. If the answer is `true`:

    git fetch --unshallow

— before drawing ANY conclusion about what is or is not merged.


─── PULL BEFORE ANCHORING ───

Never work from a transcribed branch list or a file snapshot.
Regenerate it.

**This applies with force to your own report.** Its line numbers were
re-derived at `db326d2`; master is now `3ca9498`. RE-DERIVE EVERY LINE
NUMBER against HEAD before acting on it.


─── DELETION ───

`--merged master` means deleting the branch loses no commits. It does
NOT mean the branch is unused — check whether a session is live on it.

Deleting an UNMERGED branch discards commits. When `git branch -d`
refuses, that refusal is THE TRUTH, not an obstacle. Never `-D`.


─── PREFLIGHT HOUSEKEEPING ───

`claude/spawn-campaign-handoffs-wsblrd` is fully merged to master.
Under the law it should be deleted. You cannot delete the branch you
are standing on — checkout master first, then `-d` local. The REMOTE
deletion is outward-facing: state it in your report and get Jean's
word before pushing the delete.


═══════════════════════════════════════════════════════════════════
DISCIPLINE
═══════════════════════════════════════════════════════════════════

**A read-only audit and an edit pass do not overlap.**

Not because sequence is tidy. Because every line number in an audit is
invalidated the moment an edit lands, and the resulting divergence is
indistinguishable from a real finding — both snapshots internally
correct, only the reader wrong. That cost an adversarial pass ~20
phantom errors last session, and then caught the same drift in your own
anchor-4 table.

SPAWN_1 is an edit stage. Read what you need first, then edit.

This handoff has three sub-stages with a gate each. **They land as
three commits and the gates run in order.** 1b is meaningless if 1a's
gate did not fire; 1c is meaningless if 1b's did not.


═══════════════════════════════════════════════════════════════════
SPAWN_1a — WIRE THE CENSUS
═══════════════════════════════════════════════════════════════════

`dump_entity_census` has zero callers. Everything about its cadence
exists and is dormant: `CENSUS_DUMP_INTERVAL` (spawn_engine.hpp:65,
zero reads), `SpawnEngineState::lastCensusDump_` (:154, zero reads AND
zero writes).

This is why 1a exists as its own stage. The whole campaign after this
point gates on a census, and SPAWN_5 DELETES A FUNCTION on the strength
of that census staying quiet. A silent census and a quiet census are
indistinguishable. Nothing else may proceed until the instrument
demonstrably runs.

WORK

Mirror the agent census. It is fully wired, directly adjacent, and has
three live triggers — `"boot"` (cartridge.hpp:552), `"mood-transition"`
(:948), `"periodic"` (:1215). The entity census has the exact
mirror-image scaffolding already present and unused, including its own
`trigger` parameter.

  1. The `"periodic"` site: the empty stub at cartridge.hpp:1225,
     `// Periodic entity census dump`. Four-line mirror of :1214-1222.
  2. The `"boot"` site: mirror :552.
  3. The `"mood-transition"` site: mirror :948.

REPORT, DO NOT ASSUME — at the mood-transition site, does the census
fire BEFORE or AFTER `reset_surface` (cartridge.hpp:901)? If after,
`claimed` must be 0 for every family, because `reset_surface` bulk-wipes
the registry. That is a hard assertion the instrument can make about
teardown, and it is worth having. If the ordering puts it before, say
so and leave it — do not reorder anything to get the assertion.

GATE 1a
  glaw1 GREEN, and **the census printed at all**, at least once per
  trigger. Paste the raw first print of each into your report.

  Do not proceed to 1b until you have seen output. If it does not
  print, that is the finding and the stage stops there.


═══════════════════════════════════════════════════════════════════
SPAWN_1b — REBUILD THE CONTENTS
═══════════════════════════════════════════════════════════════════

Today the census walks `footprints_[]` and calls the result entities.
It reports a proxy. Two independent registries must be printed side by
side, and they must agree.

`FamilyDispatch` gains one member:

    uint32_t (*active_count)(<ctx>);

MATCH THE EXISTING MEMBER SIGNATURE CONVENTION for the context
parameter and constness — do not invent a type. Your report established
that `FamilyDispatch` has seven members and no `traits` pointer; add an
eighth in the same shape as the rest. Prefer const if the convention
allows it.

Twelve one-liners, one per family, from your B4 table.

── COUNTS COME FROM `.active` SCANS. NEVER FROM STORED FIELDS. ──

All seven `EntitiesState` count fields are write-only — every
occurrence is a declaration, `++`, `--`, or `= 0`. A census that trusts
them trusts a number no reader has ever validated.

This buys a standing check for free: if a scan and its stored count
ever disagree, an evictor is leaking. Do not add that comparison to the
print yet — note in your report whether any of the seven diverge at
first run.

── TRAPS (your B4 findings; restated so they are in front of you) ──

  · `MAX_RIBBON_INSTANCES` and `MAX_GALLERIES` are `t7::the_board`
    namespace constants, NOT `Dim::` members. `Dim::MAX_GALLERIES`
    will not compile.
  · `Dim::ANTENNA_SLOT_OFFSET` (16) and `Dim::CUBE_SLOT_OFFSET` (8) are
    GPU-side only. Both CPU arrays are 0-based. Do not apply them.
  · ANTENNA does not share a CPU array with COLUMN. The flora trio does
    not share one either.
  · `gol_state_.active_slot_count` and `cpu_pyramids.count` are
    HIGH-WATER MARKS, not populations. Both are live-read, which is
    what makes them tempting. Do not use them.
  · GALLERY: count `gallery_centers`. `painting_slots[32]` is shared
    with `indoor_shell` via `form_type == WALL_FRAME` and would mix
    outdoor paintings with indoor wall frames.
  · The cube corral already derives an active count by scanning
    (cube_behaviors.hpp:290-292). Your accessor must agree with it.
  · ROSTER-disabled families must read ZERO ON BOTH SIDES. That
    agreement is itself a check — do not special-case them out.

── PRINT FORMAT ──

Fixed, because every later stage compares against it:

    [CENSUS t=  312.4 trigger=periodic]
      fam    active  claimed   delta
      pyr         3        3       0
      arch        5        3      -2
      ...
      TOTAL      42       45      +3    footprints 45/512

`delta = claimed - active`. The TOTAL row carries registry occupancy
against capacity, so saturation becomes visible before it bites.

Keep the per-entity detail listing (XZ and age live in the registry),
but LABEL IT `claimed ground` — not entities. It is footprint data and
must stop calling itself something else.

GATE 1b
  glaw1 GREEN. Twelve rows print. Paste a raw periodic print.


═══════════════════════════════════════════════════════════════════
SPAWN_1c — THE PREDICTION
═══════════════════════════════════════════════════════════════════

This is the stage's real product. An instrument that reports is worth
less than an instrument that can be wrong.

**Write the prediction into your report BEFORE running 1b's build.**
Commit to it in text, then run, then compare. A prediction recorded
after the fact is not a test.

PREDICTED FIRST-PRINT DELTAS

| family | predicted delta | why |
|---|---|---|
| sph, cube | 0 | they register today (SPAWN_2 drives claimed to 0) |
| arch | NEGATIVE, by the live force-spawned portal count | Channel B registers nothing |
| ribbon | POSITIVE if any 0-tip reject occurred | commit-side leak path 3 |
| gallery | POSITIVE if any 0-painting abort occurred | commit-side leak path 2 |
| ALL | MONOTONE UPWARD DRIFT over session time | leak path 1, unbounded |
| ROSTER-disabled | 0 on both sides | never selected |

The arch row is the sharpest of these. `PORTAL_DENSITY = 1.00f`, so
every DOORWAY-tier arch through dispatch is a portal AND is registered;
only `force_spawn_portal_arch` registers nothing. So the arch delta
should equal MINUS the count of live force-spawned portals — a specific
number, not a direction. If you can instrument that count cheaply,
predict the integer.

The drift prediction needs time. Let the periodic census run over
several minutes of traverse and report the TOTAL row's trend. Leak
path 1 keys footprints to a patch that does not exist, so
`unregister_footprints_for_patch` can never match them; on a linear
traverse they are reclaimed by nothing short of `reset_surface`.

WHAT A FALSIFICATION IS WORTH

If a row disagrees with its prediction, that is the most valuable
output of the entire stage. Do not reconcile it by adjusting the
prediction. Report it, name the family, and state what the disagreement
implies about the model. The campaign's next three stages are built on
this model being right.

GATE 1c
  The report carries: predicted table, actual table, and every
  divergence named.


═══════════════════════════════════════════════════════════════════
APPENDIX — SPAWN_0b: PierTier  (RULED. Separate commit. Land LAST.)
═══════════════════════════════════════════════════════════════════

Jean's ruling on anchor-7: **cut the enum and the six writes; the field
becomes explicit padding.**

Rationale, so you can check the reasoning rather than take it: 48 − 4 =
44, which is not 16-aligned. The struct-alignment law REQUIRES four
bytes of padding there. So `_pad0` is compliance, not reserved
capability, and the charter's no-speculative-structure clause is not
engaged. Stride unchanged, WGSL layout unchanged, the 48-byte assert
unchanged.

  · Delete `namespace PierTier` (state.hpp:877, nine constants).
  · Delete the six writes: grounded.hpp:678, :687;
    entity_pipeline.hpp:523, :665, :1088, :1099.
  · `GPUPierInstance.tier` (state.hpp:868) → `_pad0`, same type,
    same position. Drop the "metadata for future use" comment.
  · Mirror the rename in world.wgsl:2556 for symmetry only. WGSL field
    names are not layout-bearing; this is readability.

── THE TRAP, VERBATIM FROM YOUR OWN REPORT ──

  A casual `.tier` grep over the shader APPEARS to refute the ruling.
  world.wgsl:9896, :9935, :10064 read `p.tier` — but that is
  `p = cmg_params[slot]`, i.e. **GPUColumnMeshParams**, a different
  struct on a different numbering (`TIER_ANTENNA_FIRST = 3u` vs
  `PierTier::COL_ANTENNA = 7`). That field IS live and load-bearing:
  it selects the antenna profile and gates the indoor ceiling fit, and
  it is written at spawn_engine.hpp:384.

  Two structs, one field name, opposite verdicts. Cutting the wrong one
  changes every column and antenna in the world.

WHY THIS LANDS LAST, NOT FIRST
  It is the only item here that touches WGSL. SPAWN_1 is CPU-only and
  needs no shader recompile. Keeping them separate means an FXC
  surprise cannot block the instrument the whole campaign depends on.

GATE 0b
  glaw1 GREEN, and a shader recompile on Jean's machine. Flag it to him
  as a shader gate, not a code gate.


═══════════════════════════════════════════════════════════════════
LAND ORDER
═══════════════════════════════════════════════════════════════════

  1. Preflight: shallow check, unshallow if needed, re-derive line
     numbers against HEAD. Report the branch housekeeping position.
  2. Read what 1b needs. Write the 1c prediction. THEN edit.
  3. Commit A — SPAWN_1a (wiring, three trigger sites). Gate 1a.
  4. Commit B — SPAWN_1b (active_count + print rebuild). Gate 1b.
  5. Compare against the prediction. Gate 1c.
  6. Commit C — SPAWN_0b (PierTier). Gate 0b.
  7. Report. **Request permission before merging to master** — your
     operating instructions require it and Jean's last authorisation
     was specific to that push, not standing.

ONE end build per commit. Jean runs glaw1 and holds the visual gate.

DO NOT begin SPAWN_2 from this handoff.


═══════════════════════════════════════════════════════════════════
RECORDED, NOT FOR ACTION
═══════════════════════════════════════════════════════════════════

**Ruling 18 — `7t_theme_tool.jsx`: DEFERRED to SPAWN_3b.** Jean's call:
the tool is useful, fix it once the main program is right, and only if
it adds no noise now. It does add noise now — the tool models the
placement law, SPAWN_2 and SPAWN_3 change that law, and SPAWN_3 is
precisely what makes `MIN_SEPARATION` retuning necessary. Resyncing to
numbers that are about to move means doing it twice.

One pass, after SPAWN_3, done as an IMPORT from the C++ tables rather
than a hand resync — so a 9×9 model of a 12×12 world cannot happen a
third time. Do not touch it in this stage.

**Ruling 13 — formations: parked, last.** Unchanged.

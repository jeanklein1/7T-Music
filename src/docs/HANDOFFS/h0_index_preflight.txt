═══════════════════════════════════════════════════════════════════════
CC HANDOFF H0 — GROUND_CARD_1: INDEX + PREFLIGHT
Campaign: GROUND_CARD_1 (implements ground_card_campaign_v2.md Stages 1–4)
Branch: GROUND_CARD_1, cut from CLOSURE_GPU HEAD. Record the ACTUAL hash
in every commit message footer. Expected base: bd405d92 or a verified
descendant; if HEAD ≠ bd405d92, run the H0 anchor check below BEFORE any
edit and report drift per-anchor.
═══════════════════════════════════════════════════════════════════════

LAND ORDER: [H0] → [H1] → [H2] → [H3] → [H4] → [H5] → [H6].
glaw1 (audit/tools/glaw1/run.sh) after EVERY handoff; expected GREEN.
Commits exactly as specified per handoff (bisection law). ONE Windows
build at batch end (Jean's gate list in H6). Push only at H6.

LAWS (absolute):
- LF-only, no BOM, anywhere you touch.
- The world.wgsl banner FXC block (lines ~44–56) is UNTOUCHED.
- No behavior changes beyond campaign v2 §7's disclosure register.
- Verify-first. Two anchor classes:
  CLASS A — exact FIND/REPLACE. FIND must match EXACTLY ONCE in the
    named file. Zero matches or >1 → ABORT the handoff, report verbatim
    surroundings, continue with the NEXT handoff only if independent.
  CLASS B — anchor-by-signature. The spec names a unique signature
    (label string, function name, constant name). You locate it, PASTE
    the actual text into your log, apply the described transform, show
    before/after in the log. Ambiguity → ABORT same as A.
- Every removal of a bind entry is PAIRED: the layout entries array AND
  the matching bind-group entries array, array size N decremented,
  entries[i] indices renumbered. Never one side only.
- audit/ artifacts are your allies: probe_a.patch (ribbon-pair removal
  diff), probe_b_branch_results.json + its per-evictee commits'
  patches (the Stage-4 eviction shapes), cc3_output.json (dead-entry
  cross), cc6_layout_budgets.py (the recount you run at H6).

AMENDMENTS to campaign v2 (approved, fold silently):
1. FUTURE-LIVE entries are RETAINED, not removed-then-readded:
   Compute Entity 145 photo_heightfield / 146 photo_sampler /
   152 patch_grid, and Compute Texture 23 nearest_sampler stay in
   place through H1; their comments are corrected to the truth
   ("dead today; goes live at Stage 4 — GROUND_CARD_1"). H5 makes
   them live.
2. Card G/B = WAVES-ONLY gradient this batch (exact parity with the
   current VS normals). Pulse-shaded normals are Stage 6 work.

H0 PREFLIGHT — run before H1:
1. git fetch; checkout CLOSURE_GPU; record HEAD hash H. Cut GROUND_CARD_1.
2. Anchor check (grep, exact strings, expect exactly the stated count):
   a. realization/binding_registry.hpp: "pyramid_instances          = 30" (1)
   b. realization/binding_registry.hpp: "pawn_aura_read             = 33" (1)
   c. realization/state.hpp: "Patch Heightfield Array (225x256x256" (1)
   d. realization/renderer.hpp: "DispatchWorkgroups(4, 1, 1)" — on the
      frustum-cull dispatch (1 at that site; report any others)
   e. realization/world.wgsl: "Only flying ribbons now; no terrain-following needed." (1)
   f. realization/world.wgsl: "// §7.4 PAWN AURA" (1)
   g. realization/world.wgsl: "fn query_ground_walker(" (1)
   h. realization/world.wgsl: "fn compute_entity_placement(" (1)
   i. realization/state.hpp: desc.label strings, one each: "Compute
      Entity Layout", "Compute Texture Layout", "Entity Placement
      Compute Layout", "Photographer Compute Layout", "Frustum Cull
      Compute Layout", "Ribbon Compute Layout", "Render Texture
      Layout", "Shadow Texture Layout", "Pawn Aura Compute Layout"
3. Report the anchor table (a–i: PASS/FAIL) in the H0 log. Any FAIL:
   stop, report, await instruction. All PASS: proceed to H1.
4. NOTE for Jean (copy into the H0 log): the 5-minute Windows witness
   run (audit/probe_dawn_witness.mjs in Chrome on the design machine,
   plus the wgslLanguageFeatures query) is the campaign's remaining
   precondition; it can run any time before the batch-end build.

DELIVERABLE per handoff: a log section in GROUND_CARD_1_LOG.md at repo
root on the branch — per edit: anchor class, file, before/after (B-class),
commit hash, glaw1 result. H6 appends the recount tables.
═══════════════════════════════════════════════════════════════════════

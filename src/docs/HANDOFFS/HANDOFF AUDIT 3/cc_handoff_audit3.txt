═══════════════════════════════════════════════════════════════════════
CC HANDOFF — AUDIT-3 (post-UNIFIED_GROUND_1 pin + Stage-6/7 groundwork)
Shape: AUDIT spec — read-only on the mainline; probe branch throwaway.
Base: the post-UG1 tree (origin/UNIFIED_GROUND_1 HEAD or the trunk Jean
designates — record hash + designation in the header). Deliverable:
AUDIT3_REPORT.md, sections [A3-1]..[A3-6], verdict + recipe + verbatim
per the census convention. Probe branch: <base>_A3_P1 (shape probes
only — no removals this audit).
═══════════════════════════════════════════════════════════════════════

[A3-1] BASELINE PIN
Rerun cc6 / cc7 + mirror / cc4 / the Dawn witness (U roster) against
HEAD; diff vs the _post_ug1 outputs. EXPECTED: identical (timing fields
aside). Record Jean's UG1 gate outcomes as a checked table (build/boot
PASS reported; the finer gates as reported or UNKNOWN — no re-runs).

[A3-2] THE PARKED-NUMBER LEDGER (one table, closes the bookkeeping)
For each parked/tombstoned number: site, precedent cited, and a
tree-wide grep proving no straggler references beyond the tombstone:
g0:29 / g1:30 (LUT retirement) · g0:149 (pyramid_ground) · g0:163 /
164 / 165 / 167 / 168 / 169 (UNIFIED_GROUND_1) · ZONE_DERIVE_EXTENT
(U5b) · plus the doc-only PATCH_INDEX_COUNT note (zero runtime readers
— confirm, no edit). Any straggler is a FINDING.

[A3-3] STAGE-6 FACTS — the true-band swap's raw material (verbatim)
a. THE BAND MACHINERY POST-RETIREMENT: paste the surviving band
   evaluator chain in full — terrain_height_and_complexity (the fused
   bake body) and the per-band lattice-wave evaluation it calls:
   the frozen path, the MOVING path (phase_moving), the
   frozen↔moving mix and its gate (band_act / blend source), the
   phase-origin read (get_band_phase_origin or successor), temporal
   frequency / pool-beat fields, and the per-band constant table
   (amp / spatial freq / temporal params per band 0..5). Name which
   band index is the fine ripple (the Stage-6 ruling keeps it static)
   and the λ ordering.
b. GRADIENT FORMS: does any true-band with-gradient variant exist, or
   are gradients bake-side only? Paste the bake's TWO-PASS pattern in
   full — generate_patch_heights + generate_patch_gradients + the
   patch_height_scratch plumbing (bindings, tile sharing, dispatch
   pair) — this is the card writer's model for Stage 6.
c. THE WIRES: config band fields (band_blend[6] / band_phase_origin[6]
   — offsets, GPU struct mirror), their setters (set_band_motion /
   set_terrain_time and kin), the boot-pin block (cartridge) that
   holds them at REST_* (terrain_looks ROW 2), and the U-phase drain
   path a live wire would ride. Plus the visual_canvas sites where the
   Movement-era couplings tick (TIDE/RAIN and any band-adjacent
   harvest) — the re-aim's landing zone.
d. THE OVERLAY RETIREMENT SURFACE: post-UG1 callers of
   terrain_wave_overlay_with_gradient + the OVERLAY_WAVES table +
   WAVE_THRESHOLD kin. EXPECTED: write_live_card is the sole caller —
   the Stage-6 swap retires the overlay whole; enumerate the exact
   retirement set (table, evaluators, consts, §2.2 ROW 7 rows).
e. CARD SCRATCH BUDGET: confirm a 512² r16f/rg16f scratch texture (or
   reuse of patch_height_scratch's pattern at card size) fits the
   writer's layout budget; name the binding-number candidates (g0
   terrain band free slots post-31: expect 32/33 — verify).

[A3-4] STAGE-7 FACTS — contact collision's raw material (verbatim)
a. agent_tier_gains: the C++ struct + authored rows + upload path +
   WGSL mirror struct (field-for-field), so the radius/mass columns
   know their insertion shape.
b. AgentState: the full GPU struct with the free pad float named
   (offset + both rooms).
c. INSERTION POINTS: update_player_agent and update_other_agents —
   paste the post-step region (after the manifold snap, before the
   write-back) where the gather loop lands; note what per-agent data
   is already in registers there (pos, tier_idx, active flag).
d. THE PRECEDENT LOOP: paste flock2d's neighbor gather (the FXC-safe
   bounded-loop shape) — the contact loop mirrors it.
e. FLOATERS: sphere + cube forcefield radii constants and the
   update_sphere / update_cube bodies' candidate insertion points for
   sphere-vs-walker and cube-vs-pawn nudges.
f. DISPATCH ORDER TRUTH: the seven-dispatch order as coded (player vs
   others vs floaters) — the one-frame asymmetry note in campaign v2
   §7.9 must cite the real order.
g. MASS AUTHORITY: confirm no existing pawn-mass constant exists (the
   "pawn heavy" ratio will be authored fresh — name where it should
   live per the panel law: population_themes vs a tier column).

[A3-5] SHAPE PROBES (witness Dawn; timings; >10 s FLAG; FXC word stays
with Jean's machine)
(i) TWO-PASS CARD WRITER: a scratch pair of kernels — pass 1 writes
    per-texel band-delta heights to a 512² scratch, pass 2 reads the
    scratch neighborhood, writes vec4(h, gx, gz, a) — the bake's
    pattern at card size. Compile + validate + time.
(ii) TRUE-BAND DELTA KERNEL: a fresh standalone kernel evaluating
    Σ(bands 0..3) of (moving − frozen) by CALLING the surviving
    evaluator pieces (bounded loops, the bake's own loop shape).
    Compile + validate + time.
(iii) CONTACT GATHER: a compute kernel with a uniform-bounded 33-slot
    pair loop (distance test + capped spring accumulate — flock2d's
    shape with a different radius source). Compile + validate + time.

[A3-6] THE FEATURE LINE
If Jean has the Chrome wgslLanguageFeatures result from either Windows
session, record it verbatim + the paint-card residency consequence;
else PENDING (blocks nothing in Stages 6–7).

REPORT-BACK: AUDIT3_REPORT.md on an audit branch off base; per-section
VERDICT; probe timings tabled; no mainline edits.
═══════════════════════════════════════════════════════════════════════

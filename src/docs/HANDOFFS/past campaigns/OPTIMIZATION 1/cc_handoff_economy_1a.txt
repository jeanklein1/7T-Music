════════════════════════════════════════════════════════════════════
7T — ECONOMY_1a — THE FIRST TWO ARMS
Two held branches, cut AFTER SWEEP_1 lands on master.
E1: the curtain switch (Class I at rest). E2: shadow at the half
mesh (Class II argued, Class III binding). No design work beyond
what is specified. No opportunistic edits.
════════════════════════════════════════════════════════════════════

ORDERING LAW: execute only after cc_handoff_sweep_1 has fully landed.
Cut both branches from the post-sweep master head (CC reports it).

GIT LAW: trunk-based; two held branches:
  claude/economy-e1-curtain-switch
  claude/economy-e2-shadow-lod1
One commit each. Independent — do not base one on the other. The
COMPOSITION NOTE at the end governs their eventual joint merge.

REGISTER: verify every anchor verbatim; report anchor + count with
boundary BEFORE each edit; STOP on mismatch except where named
expected. A tripped recon gate yields a PROPOSED repair in the
report, unapplied. REPORT; never improvise.

────────────────────────────────────────────────────────────────────
ARM E1 — THE CURTAIN SWITCH (claude/economy-e1-curtain-switch)
────────────────────────────────────────────────────────────────────
THE INVARIANT: the curtain band (8,192 of 16,896 tris per LOD0 patch,
~4,096 base verts) emits zero area whenever no per-cell lift is
active. Curtains exist to seal DISCONTINUOUS per-cell lift; smooth
per-vertex fields (waves, aura) never need them. We stop submitting
what cannot appear.

RECON R1 — THE FLAG'S TRUTH CONDITION (STOP-gated). Enumerate every
WRITER of the live card's cell-lift channel (the `.a` the ug_cell_lift
path reads). Expected: the zone systems only (GoL/Pulse slots). Report
each writer with file+line. If ANY writer outside the zone systems
exists (anything that can make adjacent cells differ in lift), STOP —
the flag's condition must name it or the arm is unsound.

RECON R2 — THE EMISSION SITE. Quote the LOD0 index-buffer builder
(state.hpp, the :3159–:3278 region) far enough to show the three
emission blocks: cap quads (expected 4,096/patch-space), curtain
quads (expected 4,096), skirt ring (expected 256). Confirm the
curtain block is cleanly separable. Report the exact per-band index
counts the builder produces today.

RECON R3 — THE COUNT CARRIERS. Index count travels with the IB.
Enumerate every site that supplies the LOD0 index count: the direct
draws (main indoor path, shadow band-0, the snapshot pass — the
census located gallery.hpp:1275 binding the patch IB), and the
indirect path (the CPU reset that writes indexCount into the frustum
args — quote it). Every carrier must switch with the flag or the arm
is wrong. List them all BEFORE editing.

EDITS (after R1–R3 pass):
  1. Parameterize the existing builder — `bool with_curtains` — and
     build a second shared buffer `patchIndexBufferCapOnly_` (caps +
     skirt, no curtain block) beside the full one. Accessor + the
     createBuffers success conjunction. Do not duplicate the emission
     code; one builder, one parameter.
  2. The flag: `bool curtains_active` derived per frame from the R1
     writer set (expected: any zone slot active). Conservative by
     construction: full IB whenever a lift COULD be nonzero. Either
     flip timing is safe — the full IB is always correct; the cap-only
     IB is correct exactly when all lifts are zero.
  3. Selection at every R3 carrier: IB handle + matching index count
     chosen by the flag. The indirect reset writes the flag-selected
     count.
No shader motion. FXC exposure: zero.

IDENTITY: Class I at rest (removed quads are degenerate today; raster
output is bit-identical). Class III at zone spawn/despawn instants.
JEAN'S GATE (one boot): outdoor rest looks untouched; then provoke or
await a zone spawn and a despawn and watch the instant for any pop or
seam. One METER window at rest for the record (expect the main and
shadow rows to drop; the number is a footnote).

COMMIT: "ECONOMY_1 E1: cap-only index buffer + lift-conservative
switch (HELD — Class I at rest)"

────────────────────────────────────────────────────────────────────
ARM E2 — SHADOW AT THE HALF MESH (claude/economy-e2-shadow-lod1)
────────────────────────────────────────────────────────────────────
THE INVARIANT: the shadow pass runs the eye's decode against a target
that resolves coarser than the half-density mesh — ≥7× the vertex
work its output can express. The LOD1 index buffer already exists and
the decode is patch-agnostic; the shadow pass simply uses it for the
LOD0 band too.

RECON R4 — LIFT UNDER THE LEGACY BAND (STOP-gated). Quote the
legacy-band path of shadow_patch_terrain_vs and confirm it applies
the cell lift (card `.a`) so lifted zones still cast from the coarser
mesh (the main LOD1 path is "old topology + card.a lift" — verify the
shadow twin does the same). If the shadow legacy path lacks the lift,
STOP and propose the minimal WGSL addition in the report (that
variant would carry an FXC note and waits for Jean's word).

RECON R5 — THE TWO DRAWS. Quote draw_shadow_all's two terrain draws
verbatim (band 0: full IB × lod0Count; band 1: LOD1 IB × the ring
count at firstInstance=lod0Count).

EDIT (after R4–R5 pass): band 0 draws with `patch_index_buffer_lod1()`
and `patch_index_count_lod1`, instanceCount lod0Count, firstInstance
0. Band 1 unchanged. One SetIndexBuffer now serves both bands —
collapse the redundant re-bind if trivially adjacent; otherwise leave.
Nothing else. No shader motion (pending R4). 

IDENTITY: Class II by argument — 0.293 wu shadow texels against a
1.56 wu mesh over a band-limited height field; silhouette deltas fall
below the map's expression. Class III BINDING at the gate.
JEAN'S GATE (one boot): low sun across a lifted zone (silhouette),
contact shadow under the pawn's feet, one indoor mood's spots. One
METER window for the record (expect the shadow row to fall hard; the
number is a footnote).

COMMIT: "ECONOMY_1 E2: shadow pass draws terrain at LOD1 density
(HELD — Class II argued, gate binding)"

────────────────────────────────────────────────────────────────────
COMPOSITION NOTE — when both arms pass their gates
────────────────────────────────────────────────────────────────────
On joint merge, the shadow band-0 site takes E2's form outright; E1's
flag then governs the main pass and the snapshot pass only (a shadow
pass on the LOD1 IB has no curtains to switch). E1's shadow-side
selection from R3 becomes dead the moment E2 merges — the joint-merge
commit deletes it rather than leaving a switch with one arm. State
this in both branch reports so the merge session inherits it.

────────────────────────────────────────────────────────────────────
REPORT BACK
────────────────────────────────────────────────────────────────────
- R1 writer table; R2 emission quote + per-band counts; R3 carrier
  list; R4 verdict with quoted lines; R5 quotes.
- Both branch heads with hashes for Jean's checkouts.
- Any STOP: the proposed repair, unapplied.
The one thing this handoff must not do: touch master.

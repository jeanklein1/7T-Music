════════════════════════════════════════════════════════════════════
CC HANDOFF — PRUNING_2 / P2-A · THE DEAD DOORS
Delete eleven zero-caller setters from GPUState. One file, one commit.
════════════════════════════════════════════════════════════════════

EXPECTED TREE: c00751ad3f2de42985edb4fbfab2f23e20ace8f4   (branch master)

  If HEAD is not this commit, STOP and report. Do not adapt the
  anchors to a different tree — re-run recon instead. (The previous
  handoff failed to name a hash; that omission is corrected here.)

FILE TOUCHED: src/cartridges/the_board/realization/state.hpp  — ONLY.
  No other file changes. No boot-block change. No WGSL change. No
  DesignConfig change. Struct layout, mirror, offsets, sizeof: all
  untouched by construction — this deletes member FUNCTIONS only.

AUTHORITY: every FIND block below is transcribed from YOUR OWN
  PRUNING_2_RECON.md §D1-D4 extractions at the above commit. If a
  block does not match byte-for-byte, that is a real discrepancy —
  report it, do not repair it.

════════════════════════════════════════════════════════════════════
WHY THESE ARE SAFE — the mechanism, stated once
════════════════════════════════════════════════════════════════════

Recon §B: all eleven have ZERO callers in
`src/cartridges/the_board/**` + `src/incubator_dual.cpp`. Controls
(`set_mute_coupling` 2, `set_fpv_mode` 1, `set_world_seed` 2,
`set_checker_color_field` 2, `toggle_freeze_sphere` 1) all returned
their expected nonzero, so the zeros are trustworthy.

Recon §D0: every one of these ELEVEN FIELDS is boot-pinned by a DIRECT
`config_.<field> = …` write in `initializeState()` — the setters are
bypassed. Deleting a setter therefore orphans nothing: the pin stands,
the field keeps its value, and its WGSL readers are unaffected.

**The fields stay. Only the doors go.** Do not touch
`initializeState()`, do not touch `GPUDesignConfig`, do not touch
world.wgsl.

════════════════════════════════════════════════════════════════════
TWO HAZARDS — read before editing
════════════════════════════════════════════════════════════════════

H1 · PREFIX FAMILIES. Three names below are PREFIXES of LIVE
     functions. The FIND blocks are written to be exact, but if you
     ever fall back to a name search, the trailing `(` is mandatory:

       set_mute_coupling   (uint32_t, bool)  — 2 callers — **KEEP**
       set_mute_couplings  (uint32_t)        — 0 callers — DELETE
       set_veil            (f,f,f)           — 0 callers — DELETE
       set_veil_strength   (f)               — 1 caller  — **KEEP**
       set_veil_dither     (f)               — 1 caller  — **KEEP**
       set_freeze_sphere   (bool)            — 0 callers — DELETE
       toggle_freeze_sphere()                — 1 caller  — **KEEP**

H2 · NON-ASCII IN ANCHORS. Several anchor lines carry multibyte UTF-8
     that MUST survive byte-exact:
       `─` U+2500 in the two banner rules
       `λ`  U+03BB in set_cube_plasticity's trailing comment
     File law L1: LF-only, no BOM. `state.hpp` has NO trailing newline
     — leave it that way; none of these edits reach the last line.

════════════════════════════════════════════════════════════════════
STEP 0 — VERIFY BEFORE TOUCHING ANYTHING
════════════════════════════════════════════════════════════════════

For each of the six FIND blocks: confirm it occurs EXACTLY ONCE in
state.hpp. Report the six counts as a table. If any count is not 1,
STOP and report — do not execute a partial set.

════════════════════════════════════════════════════════════════════
BLOCK 1 of 6 — set_mute_signal + set_mute_dynamics_0d
(banner survives; set_mute_coupling below it is LIVE and stays)
════════════════════════════════════════════════════════════════════

FIND:
            // Muting
            void set_mute_signal(bool m) {
                uint32_t v = m ? 1 : 0;
                if (config_.mute_signal != v) { config_.mute_signal = v; configDirty_ = true; }
            }
            void set_mute_dynamics_0d(bool m) {
                uint32_t v = m ? 1 : 0;
                if (config_.mute_dynamics_0d != v) { config_.mute_dynamics_0d = v; configDirty_ = true; }
            }
            void set_mute_coupling(uint32_t b, bool m) {

REPLACE:
            // Muting
            void set_mute_coupling(uint32_t b, bool m) {

════════════════════════════════════════════════════════════════════
BLOCK 2 of 6 — set_mute_couplings + the orphaned "// Tuning" banner
                + set_pawn_speed
(the leading `}` is set_mute_coupling's closing brace — it is part of
 the anchor and must be reproduced in the replacement)
════════════════════════════════════════════════════════════════════

FIND:
            }
            void set_mute_couplings(uint32_t mask) {
                if (config_.mute_couplings != mask) { config_.mute_couplings = mask; configDirty_ = true; }
            }

            // Tuning
            void set_pawn_speed(float s) {
                if (config_.pawn_speed != s) { config_.pawn_speed = s; configDirty_ = true; }
            }

            // ─── The point's host + fly speed ────────────────────────

REPLACE:
            }

            // ─── The point's host + fly speed ────────────────────────

NOTE: `// Tuning` dies because `set_pawn_speed` was its only member.
A banner over nothing is a false affordance — the exact class this
campaign removes.

════════════════════════════════════════════════════════════════════
BLOCK 3 of 6 — set_point_bubble_radius + set_cube_plasticity
(both sit BETWEEN live neighbours: set_point_fly_speed above,
 set_fpv_mode below. Both stay. Mind the trailing comments' spacing:
 THREE spaces before `// CONTACT_2`, EIGHT before `// CONTACT_3`.)
════════════════════════════════════════════════════════════════════

FIND:
            void set_point_bubble_radius(float r) {   // CONTACT_2: the coupling campaign's wire
                if (config_.point_bubble_radius != r) { config_.point_bubble_radius = r; configDirty_ = true; }
            }
            void set_cube_plasticity(float p) {        // CONTACT_3 K2c: the live λ master
                if (config_.cube_plasticity != p) { config_.cube_plasticity = p; configDirty_ = true; }
            }
            void set_fpv_mode(uint32_t m) {

REPLACE:
            void set_fpv_mode(uint32_t m) {

════════════════════════════════════════════════════════════════════
BLOCK 4 of 6 — set_freeze_sphere
(the live door is toggle_freeze_sphere, immediately above; the leading
 `}` is ITS closing brace and is reproduced in the replacement)
════════════════════════════════════════════════════════════════════

FIND:
            }
            void set_freeze_sphere(bool f) {
                uint32_t v = f ? 1 : 0;
                if (config_.freeze_sphere != v) { config_.freeze_sphere = v; configDirty_ = true; }
            }

            void set_config_dynamic(bool d) { configDynamic_ = d; }

REPLACE:
            }

            void set_config_dynamic(bool d) { configDynamic_ = d; }

════════════════════════════════════════════════════════════════════
BLOCK 5 of 6 — set_veil
(H1 applies. set_veil_strength and set_veil_dither below it are LIVE.
 The VEIL banner above describes the whole family and STAYS — its
 remaining members are set_veil_strength, set_veil_dither,
 veil_ring(), lod0_radius().)
════════════════════════════════════════════════════════════════════

FIND:
            void set_veil(float ring, float icing, float lod0) {
                if (config_.veil_ring != ring || config_.veil_icing != icing ||
                    config_.lod0_radius != lod0) {
                    config_.veil_ring   = ring;
                    config_.veil_icing  = icing;
                    config_.lod0_radius = lod0;
                    configDirty_ = true;
                }
            }
            void set_veil_strength(float s) {

REPLACE:
            void set_veil_strength(float s) {

════════════════════════════════════════════════════════════════════
BLOCK 6 of 6 — the palette three + their banner
(all three members go, so the banner goes with them. The trailing
 anchor line is set_mode_palette_drift's signature — it STAYS.)
════════════════════════════════════════════════════════════════════

FIND:
            // ─── The palette mirror setters ──────────────────────────────
            void set_palette_center(uint32_t i, float r, float g, float b) {
                if (i >= 4) return;
                if (config_.palette_center[i][0] != r || config_.palette_center[i][1] != g
                    || config_.palette_center[i][2] != b) {
                    config_.palette_center[i][0] = r;
                    config_.palette_center[i][1] = g;
                    config_.palette_center[i][2] = b;
                    configDirty_ = true;
                }
            }
            void set_palette_light(uint32_t i, float r, float g, float b) {
                if (i >= 4) return;
                if (config_.palette_light[i][0] != r || config_.palette_light[i][1] != g
                    || config_.palette_light[i][2] != b) {
                    config_.palette_light[i][0] = r;
                    config_.palette_light[i][1] = g;
                    config_.palette_light[i][2] = b;
                    configDirty_ = true;
                }
            }
            void set_palette_weight(float w0, float w1, float w2, float w3) {
                if (config_.palette_weight[0] != w0 || config_.palette_weight[1] != w1
                    || config_.palette_weight[2] != w2 || config_.palette_weight[3] != w3) {
                    config_.palette_weight[0] = w0;
                    config_.palette_weight[1] = w1;
                    config_.palette_weight[2] = w2;
                    config_.palette_weight[3] = w3;
                    configDirty_ = true;
                }
            }

            void set_mode_palette_drift(float target, float intensity, float discrete_tier) {

REPLACE:

            void set_mode_palette_drift(float target, float intensity, float discrete_tier) {

NOTE: the palette FIELDS remain, boot-pinned from
terrain_looks::PALETTE_*_REST (recon §D0, state.hpp:5722-5729) and
read by WGSL. Only the never-called doors go. When a palette coupling
acquires a driver it re-enters through the VOICE bus's `set_voice`,
not through a resurrected setter.

════════════════════════════════════════════════════════════════════
EXPLICITLY OUT OF SCOPE — do not touch in this commit
════════════════════════════════════════════════════════════════════

- `enter_design_mode` / `enter_performance_mode` (state.hpp:2212-2224).
  Zero callers, immediately above BLOCK 1's banner, and INSIDE the
  blast radius of a careless edit. They are a separate RULE awaiting
  Jean. BLOCK 1's anchor begins at `// Muting`, which is BELOW them —
  verify after editing that both survive intact.
- `initializeState()` and every boot pin.
- `GPUDesignConfig`, its static_asserts, its WGSL mirror.
- `set_mode_palette_drift` and the mode channels — they retire at the
  VOICE bus re-lay, not here.
- The diagnostic rig, the test rig, the print sites — later commits.

════════════════════════════════════════════════════════════════════
ACCEPTANCE
════════════════════════════════════════════════════════════════════

A1. All six FIND counts were exactly 1 before editing.
A2. These return ZERO hits in
    src/cartridges/the_board + src/incubator_dual.cpp:
      set_mute_signal(  ·  set_mute_dynamics_0d(  ·  set_mute_couplings(
      set_pawn_speed(   ·  set_freeze_sphere(     ·  set_veil(
      set_palette_center(  ·  set_palette_light(  ·  set_palette_weight(
      set_point_bubble_radius(  ·  set_cube_plasticity(
A3. These are UNCHANGED and still return their control counts:
      set_mute_coupling( = 2 · set_veil_strength( = 1
      set_veil_dither(  = 1 · toggle_freeze_sphere( = 1
      set_fpv_mode( = 1
A4. `enter_design_mode` and `enter_performance_mode` still present,
    byte-identical.
A5. `git diff --stat` shows ONE file changed, deletions only, zero
    insertions beyond the anchor lines the replacements reproduce.
A6. state.hpp still ends without a trailing newline (unchanged).
A7. Jean runs glaw1. It must compile clean. No runtime gate needed:
    this commit cannot change behaviour — every deleted function had
    no caller, and no field it wrote lost its boot pin.

COMMIT MESSAGE:
    PRUNING_2 P2-A: delete 11 zero-caller GPUState setters

    Doors with no callers, fields untouched. Every field stays
    boot-pinned by its direct config_ write in initializeState and
    keeps its WGSL readers; only the never-called setters go, with
    the "// Tuning" and palette-mirror banners that governed nothing
    else. Counts verified against controls at c00751a
    (audit: PRUNING_2_RECON.md §B, §D0-D4).

REPORT BACK: the six pre-edit counts, the A2/A3 tables, and
`git diff --stat`. Do not proceed to any further P2 commit.
════════════════════════════════════════════════════════════════════

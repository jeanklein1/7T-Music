// GENERATED by tools/binding_gen.py from tools/binding_schema.py — do not hand-edit
//
// THE FEATURE WALLET (PROBATE_F) — the program's optional-feature
// treasury. Nothing is requested at the device except through a
// status='granted' row in the schema's FEATURES table, and no row
// is granted without a fallback ruling. Witness F-1 holds this
// emission to the schema; the boot's wallet line reads it too, so
// the request and the testimony share one home.
//
// VAULTED rows are offered by the floor device and deliberately
// unrequested — priced for a future campaign, never asked for here:
//   shader-f16 — consumer: none yet — candidate: terrain fragment ALU; fallback: the f32 shader exactly as it stands
//   texture-compression-astc — consumer: none yet — candidate: authored painting uploads (TEX_C0, parked); fallback: rgba8unorm decode-and-upload as today
//   texture-compression-etc2 — consumer: partner row of astc; fallback: as astc
//   texture-compression-bc — consumer: desktop half of the astc row; fallback: as astc
//   subgroups — consumer: none — prospective (reduction-shaped passes); fallback: n/a until a consumer is named

inline constexpr uint32_t FEATURE_WALLET_GRANTED_COUNT = 1;
inline constexpr uint32_t FEATURE_WALLET_VAULTED_COUNT = 5;

// (a) THE REQUEST LIST — the only place this program names an
//     optional feature to the device.
inline constexpr wgpu::FeatureName FEATURE_WALLET_GRANTED[] = {
    wgpu::FeatureName::TimestampQuery,   // the METER (per-pass GPU timing) — fallback: meters go silent; the frame loop is untouched
};

// (b) THE WALLET PRINT — the same rows, as the names a reader knows.
inline constexpr const char* FEATURE_WALLET_GRANTED_NAMES[] = {
    "timestamp-query",
};

This is ROSTER-1a — recon first, printed for ratification before any cutting (the handoff's explicit two-phase shape). Let me set up the branch, pin the SHA, and begin the read-only census. I'll build on the reach graph and terrain dossier I already produced.

Branch pinned at `3aac3db506e49e95a688cab9d5593f8a048cb0ec`. Now **R1 — piece enumeration**. Starting with the FAMILY_DISPATCH table (the (b) dispatch backbone):

12 families confirmed. Now the (b) dispatch call sites — where FAMILY_DISPATCH rows are iterated/invoked:

The (b) family dispatch chain runs through: select loop (spawn_engine.inl:1071-1075), place/commit (1093/1110), the per-frame mesh prepare+dispatch loops (cartridge.hpp:3265-3274), and evict (cartridge.hpp:680). Now the **non-family pieces** — mood-configured subsystems and per-frame feature ticks. Reading update()/render() and apply_mood:

Now the mood-configured subsystems (apply_mood) and the boot one-shots (initialize) — the (c) gate territory:

Now the boot one-shots in `initialize()` and the teardown guards — the (c) gate sites:

The boot one-shots live in `init_renderer` (test-rig piers, configure_orbs, agent registries, spawn_population). The `initialize()` boot-neutral writes (2719-2730) are the DRIVERLESS shader-capability writes — rider A explicitly exempts them. Now **R3 — the portal door**: do mood's force-spawn portal sites pass through FAMILY_DISPATCH?

**R3 answered: portals BYPASS gate (b).** `force_spawn_portal_at` (mood.inl:886) writes `entities_state_.arches[slot]` directly, bumps `arch_count`, sets mesh-pending, and calls `write_pier` — it never touches `FAMILY_DISPATCH[ARCH]`. So a disabled ARCH family would not stop portals; the second door is required. The single choke point is `force_spawn_portal_at` (all three spawners route through it); the spine call site is cartridge.hpp:3566.

Now **R2 — bind-group census**. Extracting the buffer→bindgroup→co-resident structure:

Let me build the full buffer↔bindgroup↔co-resident map programmatically:

Let me resolve the missing buffer names (mesh vertex buffers are bound via SetVertexBuffer, not bind groups) and complete the ownership picture:

Recon complete across R1/R2/R3. Writing the report for ratification:

ROSTER-1a Phase R recon is committed (`audit/ROSTER_RECON.md`) and pushed to the new **MOD_1_ROSTER** branch. Read-only — no source touched. Printed here for ratification before any cutting.

**Pinned SHA:** `3aac3db506e49e95a688cab9d5593f8a048cb0ec`

## R1 — the roster's key list (census rules over planning names)

**12 families** (FAMILY_DISPATCH): pyramid, arch, column, antenna, palm, cactus, blade, sphere, ribbon, cube, gol, gallery. One canonical **(b)** gate each — the select loop `FAMILY_DISPATCH[f].try_select` (spawn_engine.inl:1071); disabled there ⇒ never selected/placed/committed ⇒ count 0 ⇒ no mesh, no draws.

**5 gateable features:** pawn aura, orbs (sky dome — *distinct* from the sphere family), spot lights, indoor shell, portal.

**One hot-path caveat:** the per-frame `for f in COUNT { prepare_mesh(f) }` loop (cartridge.hpp:3265) is the only genuinely hot family site — Phase I gates it by **constexpr fold**, not a runtime `if(ROSTER[f])`, per the invariant.

**Two judgment calls needing your ruling:**
- **photographer ⊂ gallery** — one `gallery` bit gates both the family and the photographer tick; split only if you want "photographer off, paintings static."
- **agents are foundational, not a roster piece** — the pawn *is* agent slot 0, and `agentStateBuffer_` is shared across 7 bind groups; disabling agents breaks the pawn. Listed for completeness, recommended non-gateable (a hard dependency like portal↔transitions).
- (minor) test-rig piers: a debug boot one-shot — does it earn a bit?

## R2 — bind-group census (feeds ROSTER-1b)

**Cleanly separable** (own bind groups + `SetVertexBuffer` geometry): pawn aura, orbs, per-family mesh-gen. **Shared megabinds** (Compute Entity, Entity Placement) co-reside nearly every piece, and **sphere + cube share one floater buffer** — so those pieces can be left *pristine* (rider A) but not un-created without re-sectioning the shared group. That's exactly ROSTER-1b's design input.

## R3 — portal door: **NO**

`force_spawn_portal_at` (mood.inl:886) writes `entities_state_.arches[]` directly and calls `write_pier` — it **bypasses FAMILY_DISPATCH**. So the second door is required (pre-authorized). All portal spawns route through that one choke point, so the recommended second-door site is **`force_spawn_portal_at`** itself (early-return when portal disabled — catches back/finite/future spawners from one place). The **2c edge**: transitions REQUIRE portal (it's both the trigger *in* and the return path *out*) — a forbidden-config `static_assert(ROSTER.portal, ...)`.

**Awaiting your ratification of three things before Phase I cuts:** (1) the piece key list — especially photographer⊂gallery and agents-foundational; (2) the portal second-door site; (3) the 2c edge mechanism (unconditional `static_assert` vs adding a `transitions` bit). Nothing is cut until you rule.

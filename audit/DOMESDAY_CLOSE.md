# DOMESDAY — CLOSED

One page, citing artifacts, no new claims. Three batches
(`DOMESDAY_0/1/2_REPORT.md`), two enacted merges, one instrument
panel handed to the walk.

## Surveyed

- **Bindings** — LOOM's ground, extended: the schema's six original
  relations plus `immediate_size` on PIPELINES (`binding_schema.py`,
  `BINDING_LEDGER.md`).
- **Passes and submits** — 20 passes, 2 submits, 2 reconfigure sites,
  every encoder and pass named (`COMMAND_LEDGER.md`).
- **Stagings** — the eight facts per pool (`DOMESDAY_0_REPORT.md` §2).
- **Laws** — L23′ and the orb defended sites, quoted with anchors
  (`DOMESDAY_0_REPORT.md` §3).
- **Objects and backings** — 94 RESOURCES rows, every seat backing
  and pass attachment resolving, zero orphans (`binding_schema.py`
  RESOURCES; witnesses R-1/R-2/R-Label).
- **Floors** — seven NEEDS rows, one home, emitted to the boot print
  (`limits_floor.gen.inc`; witness R-3).

## Moved

Two demotions (`render_vp`, `render_camera` → uniform) · one
vertex-pull (`visible_patch_indices` → instance attribute) · one
immediate (`shadow_slot` — the dynamic-offset machinery deleted
whole; `dyn_u`/`dyn_s` 0/8 and 0/4 program-wide) · four scene groups
→ one · the reconfigure debounce (six stable frames). Wallet, quoting
MANIFEST: render V from 7 uniform / 6 storage to **8 uniform /
3 storage**; the immediates lane from unopened to **4 / 64** across
the shadow family; program-wide worsts 11/12 uniform (agents C) and
5/8 storage (compute side).

## Deeded

The heightfield `.a` reserve (standing, LOOM) · the snapshot
portfolio (`state.hpp`, A13) · the authored inventory (`state.hpp`,
A13) · the paintings fence (standing).

## Declined / withdrawn / refused by the tree

Orb bind-group swap — DECLINED under L23′, pinned at the copy kernel
(`world.wgsl`) · R5 staging retirement — WITHDRAWN in full, both
halves (unified memory; the portfolio census;
`DOMESDAY_2_REPORT.md` §2) · B4 — found pre-paid (DISCARD_0).

## Standing witnesses at every landing

LOOM's set (S-1…S-7, P-scope both arms, P-seq, S-5b, 0a…0d, W1…W4,
G2-eol) **+ M-1, M-2, C-1…C-7, R-1, R-2, R-3, R-Label** — M-2's
first run caught a live defect before the build gate could
(`DOMESDAY_2_REPORT.md` §1).

## Held, named

Caster instancing · stream_patches amortization · render bundles ·
wider indirect · **MSAA default** (pending the walk — the instrument
ships, `?msaa=`) · ImageBitmap inventory (pending a vendored port) ·
feature ids beyond the switch (glaw1-arbitrated as the port moves).

## Handover

The panel campaign opens on the agents family's **11/12 uniform
row** — relief by coalescence into few wide generated blocks
(tile_grid 16.4 KB the anchor tenant), every block born a RESOURCES
citizen. Jean's part stands in `DOMESDAY_2_REPORT.md` §5: sixteen
cells, one walk, two purse questions; the default-face ruling
follows, then FRAME_1 retires.

Nothing in the GPU logistic is unowned, unlabeled, or unwatched.

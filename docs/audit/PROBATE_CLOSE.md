# PROBATE — the closing minute

**MINUTE, born current 2026-08-17 at `78bdd55`. L28 governs it from this
line on: stamped, never edited.** A correction lands as a dated addendum
at the foot, never as a change to the text above it — including the two
words §C5 leaves open. The living law is `docs/LAWS.md`.

---

## The campaign

PROBATE set out to execute the estate of a dead compiler and put a
statute on everything the living device grants. It ran **five executing
rounds and this close** — PROBATE (main), E, SEAL, GATEHOUSE, BEQUEST,
CLOSE. The main round retired FXC's ghosts, moved `patch_params` onto
the immediates lane, gave the feature vault a wallet and ruled fourteen
demand cells; E settled the errata those units surfaced; SEAL answered a
serve incident by putting a witness in the corridor between the build
and the audience; GATEHOUSE closed the gate holes SEAL's own work
revealed; BEQUEST executed the one parked move the campaign's law
mandated and priced the one the tree refused. **Every planned unit
landed.** Two were skipped on their own conditions being false, in
writing, with the disproof recorded: SEAL's §S1 (no packaging site can
cut a shader) and §S4 (the archive is unreachable from the executor).

## The visual gate, closed

> `PROBATE_I step-4: given by Jean, 2026-08-17 — "it does display the`
> `same landscape. It works as a diagnostic." Pinned seed 1808991549,`
> `witnessed identical on arm/valhall (Pixel), nvidia/kepler ×2`
> `(Chrome, Edge), sha 3cf2e02f MATCH on all three.`

**`3cf2e02f` is a historical pin, not the current tree.** It names the
exact bytes those three consoles witnessed. The tree moved to `36fd46ab`
at BEQ_A, which edited `world.wgsl`; a future reader comparing this
digest against a live console is comparing against the wrong day, and
the `[Dist]` line's own `expected=` half is the only current authority.

**BEQ_A's visual witness is not an open slot here.** The sphere's
terrain effect is presently gated and unobservable in normal flight, so
the force-field ring cannot be looked for on the pinned seed today. That
witness is **rehomed to the sphere-effect campaign**: when the footprint
is restored, if it tracks the sphere the 12-byte copy is proven, and if
it sits at the origin the copy's source offset is wrong. The 1040-byte
layout is meanwhile witnessed statically at both ends (below); what
waits is only the moving picture.

## The pinned-seed diagnostic

One seed, one world, on every architecture. Any divergence in the world
lines or the landscape is a defect announcing itself, at no cost and
without a comparison anyone has to arrange. **Seed of record for this
purpose: `1808991549`.** It earned the name by doing the job: three
devices, two vendors, two browsers, one landscape.

## Settled by this arc

- **The serve incident** — closed by its own witness, tri-device. The
  shader's digest is computed at build and checked where the audience
  receives it; the corridor that had no testimony now has one line.
- **Firefox** — lost at B6 and nobody knew; discovered at SEAL, priced
  in the banner at GATEHOUSE, bounded by the card. Witnessed.
- **The shelf twin** — `docs/past docs/DAWN_REFERENCE.md` stamped;
  default accepted over deletion.
- **GF6** — ruled HOLD. The gate's header is its stamp; the directory
  rename stays unpaid.
- **WIT_2b's number** — speaks on every device, and says zero.
- **The two EM_ASM warnings** — extinct, and the class made unwritable:
  the lint fires before clang, so `''` inside an EM_ASM body cannot be
  committed.
- **The frame_r third passenger** — `sphere_pos` at offset 1024, the
  block at 1040 B, **both rooms witnessed by instruments sharing no
  code**: four C++ `static_assert`s and the ledger's own WGSL layout
  calculator, which derived 1040 rather than being told it.
- **BF1, claimed** under the A1 law. `render_floating` stopped claiming
  a fragment stage it never visits, and the epitaph is Table C's own:
  *"the removal of a false statement that happened to cost a slot."*
  Declared F-stage storage fell 3 → 2 on all thirteen fragment rows;
  the A1 sweep is back to zero.
- **TEX_C0** — priced at `docs/audit/TEX_C0_PRICE.md` and **HELD**. The
  original sketch is recorded as **impossible**, not merely expensive:
  the exhibition array's format is the swapchain's, promotion copies
  demand matching formats, and render targets cannot be compressed. The
  viable design and its full bill are on file.

## Standing open, each with its home

- **The Tint arm** — dormant, `tools/wgsl_gate.py` arm 3. Lights on one
  environment variable; the recipe and its guarded caveats are in the
  SEAL report and the CLOSE handoff. One build on the archive machine
  gates every future commit with the compiler family the audience runs.
- **The MSL boot witness** — written here, unexecuted. The first Mac
  available, likely exhibition hardware, runs seed `1808991549` and
  reads four lines: `Dist … MATCH`, `Shader … 0 error(s)`, zero
  validation errors, and the same landscape.
- **The Firefox watch** — tripwired, `wgsl_gate.py` arm 4. The tree
  announces the day naga ships `immediate_address_space`; nobody has to
  remember to check.
- **The sphere's terrain effect (tint + extrusion)** — gated or retired
  at an unknown campaign. **First unit of the effects arc, and BEQ_A's
  deferred witness**: restoring the footprint is also what proves the
  12-byte copy.
- **The parked estate** — `TEX_C0` (see the price sheet above), the
  id-keyed A2 price list, the agent merge (L24's named payment), and the
  SOAK watchlist.

*The CHORD_5 suspect that stood here in the draft is gone: BEQ_A
executed it. `entities[0].pos` rides `frame_r` now, and the DPR
staircase was demoted from gate to measurement before it ran.*

## The words

Two entries were owed to this minute and **are not yet given**. They land
as dated addenda below, under L28, when Jean gives them:

1. **The card's words** (§C2) — A, B, C, or his own text. The card ships
   meanwhile in its sober default (`PROBATE_SEAL3`), which is a
   designed sentence and not a placeholder; the pick replaces one string
   byte-exact.
2. **The `b491115` line** (§C5.2) — one word that the force-updated
   master discarded that history intentionally. The orphan line is
   **59 commits**, from `b9629c2 TETRIS T-CLOSE` to `b491115 LOOM_0: the
   mirror census`, and it shares **no merge base** with the line that
   became master. It is currently recoverable from this session's object
   store and from any clone predating the force-update; when those go,
   it is gone.

---

*After this the subject changes. The effects campaign inherits an estate
that is censused, priced, gated, witnessed on three screens, closed with
a minute — and carrying one more free slot than the plan promised.*

---

## Addendum — 2026-08-17, under §C5.2: there was never an orphan line

**Verdict: the `b491115` history was never at risk, and nothing was ever
discarded. The word §C5.2 asked for is not owed, because the premise
that asked for it was false.**

Jean directed that the line be preserved before being judged — tag it,
push it, then test whether it was ever unique. The preservation failed
and the test made it unnecessary, in that order.

**The preservation could not be done from here.** `git push origin
orphan/pre-realign` returns **HTTP 403** — four retries at 2/4/8/16 s,
identical each time. It is a policy denial, not a network fault: a probe
tag on an ALREADY-PUSHED commit, carrying zero new objects, is refused
the same way. **Tag pushes are denied outright in this environment**,
whatever they contain.

**The test then showed there was nothing to preserve.** Run against the
full history, `git merge-base b491115 master` returns **`b491115`
itself**: the commit is a direct ancestor of master, 142 commits back.
Every commit of the supposed orphan line is on master —
`git rev-list b491115 --not master` counts **0**. The tree test Jean
specified confirms it from the other side: `b491115^{tree}` =
`4c693eba` is found in master's history, in `b491115`, which master
contains.

**The cause of the error, named.** The session's clone was **SHALLOW** —
`.git/shallow` present, 82 commits of master locally, and `b491115`
below the graft boundary. Git could not see past it, so `merge-base`
correctly reported nothing, and `fetch` reported `(forced update)` for
the same reason. **PROBATE_SEAL's flag F15 read that truncated view as
fact** and recorded "unrelated histories, no merge base, force-discarded,
59 orphan commits". The file counts in F15 were real; **its ancestry
claim was not**, and this addendum supersedes it. `git fetch --unshallow`
raises master from 82 commits to **1684**, and the whole picture
reverses.

**It is P11, collected one final time, from the round that wrote P11's
own enforcement.** *A search that verifies something is absent is never
truncated.* F15 concluded absence — no common ancestor — from a
deliberately truncated history, and did so in the same campaign that
made truncated-absence a named law. The near-repeat here was closer
still: the first pass of this very verification reported "20 files never
present in master's history", measured against the same shallow view,
and was two minutes from being written down as a verdict.

**Consequences.** The tag was deleted rather than kept: a ref named
`orphan/pre-realign` pointing at a commit plainly on master is a false
statement in the tree, and this campaign spent five rounds removing
those. Nothing needs preserving, nothing needs pushing, and the matter
is closed — not by a ruling, but by the finding that there was nothing
to rule on.

═══════════════════════════════════════════════════════════════════════
CC HANDOFF H2 — STAGE 1b: THE FRUSTUM FIX + THE TEXT FOLD
One code fix (a live bug), then comment/label truth. No other behavior.
═══════════════════════════════════════════════════════════════════════

COMMIT [2a] — the frustum-cull dispatch fix (LIVE BUG, audit CC-8a)
1. CLASS A — realization/renderer.hpp (~517–518):
   FIND (two consecutive lines; capture exact indentation):
     // ceil(MAX_ACTIVE_PATCHES / 64) = ceil(225/64) = 4
     [next line containing] pass.DispatchWorkgroups(4, 1, 1);
   REPLACE with:
     // ceil(MAX_ACTIVE_PATCHES / 64) — derived, never hardcoded again
     // (was hardcoded 4 = 256 threads vs 289 slots: slots 256–288 were
     //  never culled at full window — audit CC-8a).
     pass.DispatchWorkgroups((Dim::MAX_ACTIVE_PATCHES + 63u) / 64u, 1, 1);
   If Dim:: is not visible at this site, use the spelling this file
   already uses for MAX_ACTIVE_PATCHES (grep the file; state.hpp defines
   it — renderer.hpp includes state.hpp per the cohort). CLASS B if the
   two lines are not adjacent exactly as above.
   DISCLOSURE (log): at a full 289-slot window this may visibly restore
   patches that were previously never culled/emitted — correctness delta,
   campaign v2 §7.7.

COMMIT [2b] — the text fold
2. CLASS A — realization/state.hpp:3538-region:
   FIND: Patch Heightfield Array (225x256x256, RGBA16Float)
   REPLACE: Patch Heightfield Array (289x256x256, RGBA16Float; 289 = Dim::MAX_ACTIVE_PATCHES)
3. CLASS A — state.hpp:3557-region:
   FIND: Patch Cell Color Array (225x16x16, RGBA8Unorm)
   REPLACE: Patch Cell Color Array (289x16x16, RGBA8Unorm; 289 = Dim::MAX_ACTIVE_PATCHES)
4. CLASS B — realization/binding_registry.hpp banner (~line 28): the
   sentence citing "The 108 WGSL @binding literals in world.wgsl".
   Replace the number claim with the audited truth:
     "The WGSL @binding literals in world.wgsl (102 declarations over 97
      slots — audit cc7; five documented fc_ aliases share slots)"
   Keep the rest of the sentence intact.
5. CLASS B — state.hpp ~727, GPURibbonRingTransform field comment:
   FIND signature: the terrain_y member's trailing comment
   ("tile-modified terrain height").
   REPLACE comment with: "always 0.0 — flying ribbons (no terrain
   follow); field retained for 48-byte stride, removal is a cleanup-
   campaign item (VS input layout stride)."
6. CLASS B — state.hpp ~722, the struct banner naming `update_world`:
   replace `update_world` with the current split entry-point names
   (paste the actual banner in the log; keep its shape).
7. CLASS B — world.wgsl ~889, RibbonRingTransform mirror comment
   ("tile-modified terrain height at center XZ") → same truth as edit 5,
   WGSL room phrasing.
8. DO NOT touch: the Pawn Aura tile_grid entry or its "archetype lookup"
   comment (audit CC-8d: LIVE, refuted seed). DO NOT touch g0:29 / g1:30
   tombstones.

glaw1 after [2b]. Log per edit.
═══════════════════════════════════════════════════════════════════════

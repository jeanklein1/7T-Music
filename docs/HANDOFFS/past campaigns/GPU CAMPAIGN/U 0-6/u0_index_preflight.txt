═══════════════════════════════════════════════════════════════════════
CC HANDOFF U0 — UNIFIED_GROUND_1: INDEX + PREFLIGHT
Campaign: UNIFIED_GROUND_1 (campaign v2 Stage 5, designed on AUDIT-2)
Branch: UNIFIED_GROUND_1, cut from the post-batch mainline (expected
GROUND_CARD_1 HEAD 1240bece or the trunk Jean designates). Record the
actual hash in every commit footer.
═══════════════════════════════════════════════════════════════════════

LAND ORDER: [U0] → [U1] → [U2] → [U3] → [U4] → [U5] → [U6].
glaw1 after every handoff. One Windows build at batch end (U6 gate
list). Push at U6. Laws identical to H0 (anchor CLASS A/B, LF/no-BOM,
FXC banner untouched, paired layout+group edits, abort-on-ambiguity,
per-edit log in UNIFIED_GROUND_1_LOG.md).

ANCHOR SOURCE: AUDIT2_REPORT.md §A2-3/§A2-4 carries the verbatim
extracts with file:line for every load-bearing site — treat those
extracts as the FIND baseline (re-verify each in-tree before editing;
the report is the map, the tree is the law). The probe artifacts are
edit models: A2_P1_cleanup.patch (U1), A2_P2_stage5_retirement.patch
(U4), the two A2-5 prototype shapes (U2/U3's code forms).

MID-BATCH VISUAL STATES (accepted; one end build): after U3 the terrain
lifts under living cells WHILE the old extrusion still draws (a one-
handoff double-render); U4 retires the old stack. Bisection keeps every
state buildable; only U6's tree is judged visually.

DESIGN SUMMARY (the three-line refresher):
- Topology: new index bands stacked ABOVE the legacy vertex-index space
  — legacy grid+skirt [0,4481) keeps its decode (becomes the LOD1/soft
  space); CAP band [4481,10881) = 256 cells × 25 cell-owned verts;
  BASE band [10881,14977) = 256 × 16 curtain-bottom twins. LOD0 IB
  rebuilds (~50,688 indices); LOD1 IB is BYTE-UNTOUCHED.
- Lift: every vertex's GoL lift = card.a nearest at ITS cell, ×
  (1 − pawn suppression), except BASE verts (no lift — that gap IS the
  curtain). Rest ⇒ lift 0 ⇒ caps coincide with ground, curtains
  degenerate ⇒ bitwise rest identity.
- One surface, one color law: the terrain FS colors the caps (tint
  stack unchanged); the extrusion stack retires per the A2-3 census +
  cascade.

AMENDMENT REGISTER (Jean may veto by dropping the named handoff):
- MASK MECHANISM (U5): height_factor is CPU-Gaussian-seeded at commit
  (A2-4f finding — no kernel writer exists). Amended mechanism: a new
  zone_seed_mask kernel rides the EXISTING derive flush, multiplying
  the vocabulary predicate into the plane via the color system's own
  evaluators. Veto ⇒ skip U5's mask half; the mask defers to Layer E.

U0 PREFLIGHT:
1. Cut branch; record base hash; confirm it contains GROUND_CARD_1
   [5c] (grep: live_card_write in binding_registry) and the AUDIT-2
   instruments under audit/.
2. Anchor check (expect exactly the stated counts):
   a. world.wgsl "const PATCH_GRID_VERT_COUNT" (1)
   b. world.wgsl "fn patch_skirt_grid" (1)
   c. world.wgsl "fn zone_gol_mesh_gen" (1) — dies at U4
   d. world.wgsl "ZONE_SUPPRESS_INNER" (1)
   e. state.hpp "LOD-0: full 64×64 mesh (24576 indices)" (1)
   f. state.hpp "PATCH_MESH_N_LOD1 = 32" (1)
   g. gol_zones.hpp "HEIGHT_FACTOR_MEAN" (1)
   h. drawable_table.hpp "{ \"zone\"," (1)
   i. bodies/gallery.hpp "/*zone_active=*/false" (1) — the P2-found
      fifth DrawBind site
3. Report the table; all PASS → U1.
═══════════════════════════════════════════════════════════════════════

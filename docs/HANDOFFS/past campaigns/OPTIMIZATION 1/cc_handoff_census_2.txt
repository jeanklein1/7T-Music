════════════════════════════════════════════════════════════════════
7T — CENSUS_2 — THE RULED DISPOSITIONS
Four commits to master + one read-only run. Jean has ruled on the
CENSUS_1 seed-7..10 report; this handoff executes those rulings and
nothing beyond them. No design work. No opportunistic edits.
════════════════════════════════════════════════════════════════════

GIT LAW: trunk-based, master. All commits in this handoff land on
master directly (or the harness's `claude/*` session branch — say
which; Jean retargets). Commit order as numbered. Do not squash.

REGISTER: verify every anchor VERBATIM before editing; the line
numbers below come from CENSUS_1_REPORT.md and are trusted but must
be re-confirmed at edit time (the tree gained C1 since the census).
Report each anchor + its count with the boundary used, BEFORE the
edit. STOP on mismatch except where a mismatch is named as expected.
REPORT findings; never improvise fixes.

FXC NOTE: commit 2 removes one pipeline (gen_terrain_indices); no
new pipelines anywhere. Jean's next boot is glaw1 for all of this —
CC does not build.

────────────────────────────────────────────────────────────────────
COMMIT 1 — CENSUS_2a: THE CLASS-A PROSE (one commit, zero behavior)
────────────────────────────────────────────────────────────────────

E1 — the :414 falsity (seed 8; the ONLY one of the seven homes edited
now — the claim itself awaits adjudication by PROBE arm C3).
The sentence
  // One DrawIndexedIndirect budget line free in this pass (L2.4).
appears at EXACTLY 2 sites (render_passes.hpp:342 shadow, :414 main).
Verify count = 2. Edit ONLY the main-pass instance (the one adjacent
to the main pass's draw_table call), leaving the shadow twin intact:

REPLACE (main-pass instance only):
  // Indirect budget (L2.4): consumed outdoors by the LOD0 terrain
  // draw; free indoors.

E2 — seed 10, the honest comment. world.wgsl:3251-3253, verbatim:
FIND:
// Snap pawn XZ to shadow grid for temporal stability.
// Shadow map content is pixel-perfect between grid crossings,
// enabling the CPU to skip the shadow pass on idle frames.
REPLACE:
// Snap pawn XZ to shadow grid for temporal stability.
// Between grid crossings the light VP is bit-stable, which WOULD let
// the CPU reuse static shadow content — no skip is implemented; the
// pass runs unconditionally every frame (the lever: GEOMETRY_2).

E3 — seed 7, the mirror notes (NUMBER-FREE by design, so the held
probe1-c2 branch's FINDs on the bare declarations stay count-1).

state.hpp — insert ABOVE `constexpr uint32_t SHADOW_MAP_SIZE = 4096;`
(under // Lighting), matching local comment style:
  // TWIN: world.wgsl `const SHADOW_MAP_SIZE: f32` (— Shadow
  // constants). The WGSL twin feeds BOTH PCF texel_size reads.
  // Change it in BOTH rooms or the PCF world-footprint silently
  // rescales while the texture resizes. (L3 MIRROR — the
  // TILE_GRID_CAPACITY pattern; no compile-time bridge spans the
  // runtime-loaded seam.)

world.wgsl — insert ABOVE `const SHADOW_MAP_SIZE: f32 = 4096.0;`:
  // TWIN: state.hpp Dim::SHADOW_MAP_SIZE (// Lighting) — sizes the
  // two depth textures and the atlas tiles. Change BOTH rooms
  // together. (L3 MIRROR.)

E4 — seed 7, de-number the three prose restatements:
  render_passes.hpp:261  trailing `// 2048`  ->  `// half width`
  render_passes.hpp:262  trailing `// 4096`  ->  `// full height`
  world.wgsl:3657 line beginning
    `// Two 4096×4096 depth textures, each split left/right into 2048×4096 tiles`
  -> `// Two full-size depth textures, each split left/right into half-width tiles`
  (verify the exact in-tree characters; edit only the numbered line
  of that comment block).

COMMIT 1: "CENSUS_2a: Class-A prose — :414 corrected, shadow-skip
claim honest, SHADOW_MAP_SIZE mirror notes, restatements de-numbered"

────────────────────────────────────────────────────────────────────
COMMIT 2 — CENSUS_2b: THE SEED-9 CLUSTER (one commit, whole cluster)
────────────────────────────────────────────────────────────────────
Ruling: DELETE. Every member is exclusive to the cluster (census-
verified); re-verify each reader set with fresh word-boundary greps
AT DELETE TIME and report the counts. STOP if any count changed.

GATE G1 (before anything): the t1/t2 timing brackets at
cartridge.hpp:498/:516. Design-side reading: they almost certainly
feed the boot line `[Cartridge] Terrain gen:` (the ~1-2 ms figure in
every boot log matches this one-shot's encode cost). TRACE it. If the
pair feeds ONLY that print: delete the print with the block and note
in the report that the boot line disappears by design. If it feeds
anything else: STOP and report.

GATE G2: grep the SPINE tables and static_asserts for any row naming
the one-shot dispatch. Census placed it outside the spine; re-verify.
STOP if named.

Deletion manifest (census sites; re-confirm lines):
  D1  cartridge.hpp:502-516  one-shot dispatch block (+ its print,
      per G1)
  D2  renderer.hpp            gen_terrain_indices pipeline creation
      block + Entry::GEN_TERRAIN_INDICES constant + the dispatch
      helper
  D3  state.hpp               member :1686, accessor :2552 (zero
      callers — re-verify), creation :3132-3136 INCLUDING the boot
      gate `if (!terrainIndexBuffer_) return false;`,
      terrain_mesh_workgroups() :2952, Terrain Index Gen layout
      :4213 + bind group :5155, Dim::TERRAIN_INDEX_COUNT /
      TERRAIN_MESH_VERTS :50-52 (zero other consumers — re-verify)
  D4  world.wgsl              kernel :7862-7879, `@group(0)
      @binding(22)` declaration :5457, TERRAIN_MESH_N /
      TERRAIN_MESH_STRIDE consts :215-216 (only-kernel consumers —
      re-verify)
  D5  binding_registry.hpp:16 recompute the witnessed count
      ("96 declarations over 93 slots") with a stated boundary after
      D4; update the sentence; put old->new numbers in the commit
      message
  D6  LAWS.md:113             the g0:22 worked example. Quote the L6
      section VERBATIM in your report FIRST. Re-aim the example at a
      live g0 storage-buffer binding of the same teaching shape
      (suggest g0:340 fc_patches, or the cleanest reader). Show the
      LAWS.md diff prominently at the top of the report — Jean
      pre-authorized the swap; the wording carries his stamp at
      review. STOP only if the surrounding law text makes g0:22
      load-bearing beyond illustration.

COMMIT 2: "CENSUS_2b: retire the terrainIndexBuffer_ cluster (dead
IB + kernel + pipeline + bindings); registry recount; L6 example
re-aimed"

────────────────────────────────────────────────────────────────────
COMMIT 3 — THE DAWN REVISION LINE (verify-then-land-or-skip)
────────────────────────────────────────────────────────────────────
The Dawn checkout is unpinned (CMakeLists.txt:13); measurements do
not carry their platform. If this tree's Dawn exposes a version/hash
symbol reachable from the existing includes (check
dawn/common/Version_autogen.h — kDawnVersion — or any
dawn::native accessor; READ the headers, do not guess), add ONE line
to the C1 log block in console.hpp initWebGPU:
  std::cout << "[Console] Dawn revision: " << <symbol> << "\n";
If no symbol is reachable without new include paths or build motion:
SKIP, and report exactly what exists and where.

COMMIT 3 (if landed): "PROBE_1: log Dawn revision at boot"

────────────────────────────────────────────────────────────────────
RUN 4 — THE PARENT CENSUS (read-only; one report commit)
────────────────────────────────────────────────────────────────────
Jean will place the parent handoff file `cc_handoff_census_1.txt`
into `src/docs/HANDOFFS/OPTIMIZATION 1/` beside the addendum. Commit
it as its own small commit ("CENSUS_1: parent handoff joins the
tree"), then EXECUTE it per its own instructions — reading only,
seeds 1-6 plus the full [1]-[4] corpus walk — with ONE amendment,
ruled by Jean:

  [2] SCOPE AMENDMENT: `src/console/**` is IN.

Method stays the parent's: one reader, one refuter. Extend
audit/CENSUS_1_REPORT.md: restructure as [corpus, file-by-file] +
[seeds 1-6] + [the existing seeds 7-10 section], preserving the
existing text. One commit for the extended report.

COMMIT 5: "CENSUS_1: parent census — corpus + seeds 1-6 (console.hpp
in scope)"

────────────────────────────────────────────────────────────────────
REPORT BACK
────────────────────────────────────────────────────────────────────
- Every anchor's in-tree form + count.
- G1/G2 verdicts; the LAWS.md diff at top.
- Registry recount old->new with boundary.
- Dawn-revision verdict (landed / skipped, and why).
- Parent-census report per its own format.
The one thing this handoff must not do: touch anything the rulings
above did not name.
